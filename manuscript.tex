\documentclass[letterpaper, journal, twoside]{IEEEtran}

\def\cmt{true} % true for comments, false for no comments
\def\pub{false} % true for publication, false for draft
\newcommand*{\template}{template} 
\input{\template/preamble/preamble_journal.tex}
\allowdisplaybreaks

% --------------------------------
%      Manuscript version
% 0.9: 
% 0.91
\newcommand{\version}{0.91} % version of the manuscript
% --------------------------------

\newcommand{\resultFig}{./src/figures} % result figure path

%% RMSE values for real-time experiments
% RMSE: 
% -------------------------------
% C NAC :
% error q1 (1) 19.738, (2) 18.520 [imp: 0.062]
% error q2 (1) 10.561, (2) 12.435 [imp: -0.177]
% filter error   (1) 113.671, (2) 102.581 [imp: 0.098]

\newcommand{\NACEpiOneAngOne}{19.738}
\newcommand{\NACEpiOneAngTwo}{10.561}
\newcommand{\NACEpiTwoAngOne}{18.520}
\newcommand{\NACEpiTwoAngTwo}{12.435}
\newcommand{\NACImpAngOne}{-6.174}
\newcommand{\NACImpAngTwo}{17.745}
% -------------------------------
% C AUX :
% error q1 (1) 20.991, (2) 17.314 [imp: 0.175]
% error q2 (1) 8.268, (2) 2.421 [imp: 0.707]
% filter error   (1) 116.541, (2) 95.281 [imp: 0.182]

\newcommand{\AUXEpiOneAngOne}{20.991}
\newcommand{\AUXEpiOneAngTwo}{8.268}
\newcommand{\AUXEpiTwoAngOne}{17.314}
\newcommand{\AUXEpiTwoAngTwo}{2.421}
\newcommand{\AUXImpAngOne}{-17.515}
\newcommand{\AUXImpAngTwo}{-70.715}
% -------------------------------
% C CONAClow :
% error q1 (1) 15.984, (2) 11.190 [imp: 0.300]
% error q2 (1) 8.883, (2) 2.377 [imp: 0.732]
% filter error   (1) 90.236, (2) 63.587 [imp: 0.295]

\newcommand{\CONAClowEpiOneAngOne}{15.984}
\newcommand{\CONAClowEpiOneAngTwo}{8.883}
\newcommand{\CONAClowEpiTwoAngOne}{11.190}
\newcommand{\CONAClowEpiTwoAngTwo}{2.377}
\newcommand{\CONAClowImpAngOne}{-29.991}
\newcommand{\CONAClowImpAngTwo}{-73.244}
% -------------------------------
% C CONAChigh :
% error q1 (1) 16.836, (2) 11.492 [imp: 0.317]
% error q2 (1) 9.086, (2) 3.144 [imp: 0.654]
% filter error   (1) 94.327, (2) 65.886 [imp: 0.302]

\newcommand{\CONAChighEpiOneAngOne}{16.836}
\newcommand{\CONAChighEpiOneAngTwo}{9.086}
\newcommand{\CONAChighEpiTwoAngOne}{11.492}
\newcommand{\CONAChighEpiTwoAngTwo}{3.144}
\newcommand{\CONAChighImpAngOne}{-31.741}
\newcommand{\CONAChighImpAngTwo}{-65.402}
% -------------------------------
\newcommand{\zoomStartTime}{26.6}
\newcommand{\zoomEndTime}{27.3}

% ================================
% Document starts from here
% ===============================
\begin{document}
\bstctlcite{IEEEexample:BSTcontrol}

\title{
  Constrained Optimization-Based Neuro-Adaptive Control (CONAC) for Unknown Systems Under Multiple Convex Input Constraints
} %% Article title

\author{
  Myeongseok Ryu, Donghwa Hong, and Kyunghwan Choi
\thanks{
  Myeongseok Ryu, Donghwa Hong and Kyunghwan Choi are with the Cho Chun Shik Graduate School of Mobility, Korea Advanced Institute of Science and Technology (KAIST), Daejeon 34051, Korea (e-mail: \{dding\_98, fairytale, kh.choi\}@kaist.ac.kr).
  
  This work was supported in part by a Korea Research Institute for Defense Technology planning and advancement (KRIT) grant funded by Korea government DAPA (Defense Acquisition Program Administration) (No. KRIT-CT-22-087, Synthetic Battlefield Environment based Integrated Combat Training Platform) and in part by the National Research Foundation of Korea (NRF) grant funded by the Korea government (MSIT) (RS-2025- 00554087). (Corresponding author: Kyunghwan Choi)
  }}

% \markboth{Journal of \LaTeX\ Class Files,~Vol.~18, No.~9, September~2020}%
% {How to Use the IEEEtran \LaTeX \ Templates}
\markboth{\cred{\textit{(Version \version)}} Submitted to IEEE Transactions on Systems, Man, and Cybernetics: Systems}%
{\cred{\textit{(Version \version)}} Ryu \MakeLowercase{\textit{et al.}}: CONAC for Unknown Systems Under Multiple Convex Input Constraints}

\maketitle

%% Abstract
\begin{abstract}
  This study presents a constrained optimization-based neuro-adaptive control (CONAC) for a class of unknown multi-input-multi-output (MIMO) systems subject to multiple convex input constraints. 
  A deep neural network (DNN) is employed to approximate the ideal control law while handling multiple convex input constraints within a unified constrained optimization framework.
  The adaptive variables, including the DNN weights and Lagrange multipliers are updated through adaptation laws derived from the formulated constrained optimization problem, yielding Karush-Kuhn-Tucker (KKT)-like first-order optimality conditions at equilibrium.
  The controller's stability is rigorously analyzed using Lyapunov theory, establishing the uniform ultimate boundedness (UUB) of the tracking errors and adaptive variables.
  The proposed controller is compared with existing methods through real-time experiments on a 2-degree-of-freedom (DOF) robotic manipulator, highlighting its superior capability to handle input saturation and its feasibility in real-time implementation.
\end{abstract}

\begin{IEEEkeywords}
  Neuro-adaptive control, constrained optimization, deep neural network,  multiple convex input constraints.
\end{IEEEkeywords}

% ------------------------------------
%       SECTION INTRODUCTION 
% ------------------------------------
% \vspace{100mm}                
\section{Introduction}

\subsection{Background}

\IEEEPARstart{F}{rom} a practical perspective, many engineering systems are constrained by various safety requirements, actuator limitations, and physical restrictions \cite{Ji:2025aa}.
These constraints often manifest as convex input constraints, which can be in the form of nonlinear constraints rather than simple box constraints that limit each input independently, \ie $\underline{u}_i\le u_i\le\overline{u}_i$, $\forall i\in\{1,\cdots,n\}$, where $\underline{u}_i,\overline{u}_i\in\R$ denote the lower and upper bounds of the $i$th input $u_i$, respectively.
For example, under road friction limits, the admissible global force and moment of vehicles can be characterized by a weighted norm bound, which yields a convex ellipsoidal constraint \cite{Chen:2024aa}.
In motor drive systems, the feasible voltage region is often represented by circular \cite{Ryu:2025aa} or hexagonal sets \cite{Monzen:2026aa}.
Furthermore, admissible input sets may be determined by multiple constraints that must hold simultaneously, such as box constraints combined with norm constraints, which can be represented as the intersection of convex sets.
Therefore, addressing these multiple convex input constraints is crucial for ensuring the safety and reliability of control systems in real-world applications.

In parallel, engineering systems often involve uncertain or even unknown dynamics due to modeling errors, unmodeled dynamics, and external disturbances.
Although adaptive control provides a systematic mechanism for online adaptation of controllers \cite{Ioannou:2006aa}, conventional formulations often rely on structural assumptions about the uncertain or unknown dynamics, which may not be available in practice.
Neuro-adaptive control (NAC) alleviates this assumption by using neural networks (NNs) to approximate the dynamics in a structured and parameterized representation \cite{Farrell:2006aa,Patil:2022aa}.
Like adaptive control, NAC methods ensure closed-loop stability using Lyapunov theory, and the NN weights are adapted online without prior knowledge of the system.
The NAC methods have shown promising results in various real-world applications, including vehicle control \cite{You:2023aa} and flexible robotic manipulators \cite{Gao:2026aa}.

Because multiple convex input constraints and uncertain or unknown dynamics often coexist in real-world applications, incorporating multiple input constraint handling into NAC is essential.
This motivates a review of existing input constraint handling methods in NAC and related constrained control frameworks.

\subsection{Literature Review}

% ================================
% STRUCTURAL METHODS
% ================================

In Table~\ref{table:intro:comparison}, we summarize input constraint handling methods reported in the literature, which can be broadly categorized into structural methods and optimization-based methods.
In subsequent subsections, these methods are reviewed in the context of NAC, highlighting their advantages and limitations.

\subsubsection{Structural Methods for Input Constraint Handling in NAC}

Since the NAC framework is based on Lyapunov theory, structural methods have been widely studied for input constraint handling.
In \cite{Ji:2025aa}, Ji \etal categorized the literature on input constraint handling into two strategies: anti-saturation designs and direct designs.
We review these strategies in the context of NAC.

In anti-saturation designs, a virtual auxiliary system is mainly constructed to compensate for the effects of input saturation \cite{Ma:2025aa,Gao:2026aa,Yang:2021aa,Jia:2020aa,Liu:2024aa,Liu:2025aa,Gao:2017aa,Esfandiari:2014aa,Esfandiari:2015aa,Yu:2022aa,Rezaei:2025aa,He:2016aa,He:2019aa,Zhao:2016aa,Peng:2020aa}.
Owing to their high compatibility, auxiliary systems have been adopted in various control designs.
For example, auxiliary systems have been incorporated into backstepping-based NAC \cite{Gao:2017aa} and sliding mode control (SMC)-based NAC \cite{Ma:2025aa} to mitigate the effects of input saturation.
Furthermore, the auxiliary systems have been integrated into reinforcement learning (RL)-based NAC designs \cite{Gao:2026aa} and barrier Lyapunov function (BLF)-based NAC designs \cite{Yang:2021aa}.
To achieve finite-time or fixed-time convergence, novel finite-time \cite{Jia:2020aa} or fixed-time \cite{Liu:2024aa,Liu:2025aa} auxiliary systems have been proposed.

Generally, when the control input reaches the saturation limit, the auxiliary system is excited, and the auxiliary state is fed back into the control law as additional compensation terms.
However, from a learning perspective, if the auxiliary state is not incorporated into the feedback error used in the adaptation law, the NN weights may generate excessive control input to achieve the desired tracking performance, since the saturation effect is not considered in the adaptation process.
Therefore, the auxiliary state is often considered in the adaptation law  \cite{Gao:2017aa,Esfandiari:2014aa,Esfandiari:2015aa,Liu:2024aa,Liu:2025aa,Yu:2022aa,Rezaei:2025aa}.
This ensures that the NN weights adapt not only for approximating the ideal control law but also for compensating for the effect of input saturation.

On the other hand, in direct design, the saturation effects are directly incorporated into the controller design and stability analysis.
Typically, the saturation function is approximated by a smooth function, such as $\tanh(\cdot)$, and the mean value theorem is used to transform it into affine form \cite{Wang:2013aa,Cui:2014aa,Li:2024aa}.
However, as reported in \cite{Min:2021aa}, the smooth approximation of the saturation function may introduce conservative behavior and performance degradation, especially when the control input is close to the saturation limit, compared to the auxiliary system-based anti-saturation design.

Despite their effectiveness, the aforementioned structural methods are limited to simple box input constraints, such as symmetric or asymmetric bounds on each input.
Thus, extending them to admissible input sets described by multiple convex constraints is not straightforward.

% For instance, in \cite{Gao:2006aa}, the saturated amount of control input is estimated using NNs and compensated in the control law.

% ================================
% OPTIMIZATION-BASED METHODS
% ===============================

\begin{table*}[t]
  \renewcommand{\arraystretch}{1.3}
  \caption{
    Comparison of input-constraint handling approaches.
  }
  \centering
  \begin{tabular}{c c c c c }
  % \begin{tabular}{c c c c c}
  \hline
  & 
  \textbf{Structural Methods} 
  & 
  \textbf{Optimization-Based Methods} 
  & 
  \textbf{Proposed Approach}
  \\
  \hline
  \hline % -----------------------------------
    Representative 
  & 
  Auxiliary system design \cite{Gao:2017aa,Ma:2025aa,Gao:2026aa,Yang:2021aa,Jia:2020aa,Liu:2024aa,Liu:2025aa,Esfandiari:2014aa,Esfandiari:2015aa,Yu:2022aa,Rezaei:2025aa,He:2016aa,He:2019aa,Zhao:2016aa,Peng:2020aa},
  &
  MPC \cite{Greune:2011aa,Bemporad:1999aa,Berberich:2025aa,Draeger:1995aa,Vahidi-Moghaddam:2025aa,Mallick:2025aa,Khodaverdian:2025aa},
  &
  Preliminary works of this study
  \\
  literature
  &
  Smooth approximation of saturation \cite{Wang:2013aa,Cui:2014aa,Li:2024aa}
  &
  CBF-QP filter \cite{Ames:2019aa,Ko:2025aa,Sweatland:2025aa}
  &
  \cite{Ryu:2025ab,Ryu:2025aa}
  \\
  \hline % -----------------------------------
  \multirow{2}{*}{
    Stability analysis 
  }
  & 
  \multirow{2}{*}{
    \textbf{Lyapunov-based stability guarantees}
  }
  & 
  Conditions on feasibility and
  & 
  \multirow{2}{*}{
    \textbf{Lyapunov-based stability guarantees}
  }
  \\
  &
  &
  related assumptions required
  &
  \\
  \hline % -----------------------------------
  Input constraints 
  & 
  Primarily element-wise 
  & 
  \textbf{General input constraints}
  &
  \multirow{2}{*}{
  \textbf{Multiple convex input constraints}
  }
  \\
  handling capability
  &
  input constraints
  &
  (convex/non-convex constraints)
  &
  \\
  \hline % -----------------------------------
  \multirow{2}{*}{
    Implementation
  }
  &
    \textbf{Continuous adaptation}
  &
  \multirow{2}{*}{
    Optimization at each time instant
  }
  &
    \textbf{Continuous adaptation}
  \\
  &
  (typically Lyapunov-based)
  &
  &
  (constrained optimization-based)
  \\
  \hline
  % \multirow{2}{*}{
    Computational
  % }
  &
  \multirow{2}{*}{
    \textbf{Typically low}
  }
  &
  Problem-dependent
  &
  \multirow{2}{*}{
    \textbf{Typically low}
  }
  \\
  burden
  &
  &
  (may require iterative solvers)
  &
  \\
  \hline
  \end{tabular}
  \label{table:intro:comparison}
\end{table*}


% \cite{Greune:2011aa,Bemporad:1999aa,Berberich:2025aa,Draeger:1995aa,Vahidi-Moghaddam:2025aa,Mallick:2025aa,Khodaverdian:2025aa}, \cite{Ames:2019aa,Ko:2025aa,Sweatland:2025aa}

% MPC 
% Bemporad:1999aa,Berberich:2025aa,Draeger:1995aa,Chen:2024aa,Cetin:2019aa,Vahidi-Moghaddam:2025aa

\subsubsection{Optimization-Based Methods for Input Constraint Handling}

Besides structural methods, optimization-based methods have been studied for constraint handling across a broad range of control design frameworks.
These methods compute the control input by solving constrained optimization problems at each time instant, as in model predictive control (MPC) \cite{Vahidi-Moghaddam:2025aa,Mallick:2025aa,Khodaverdian:2025aa,Greune:2011aa,Bemporad:1999aa,Berberich:2025aa,Draeger:1995aa} and control barrier function (CBF)-based quadratic programming (QP) filters \cite{Ames:2019aa,Ko:2025aa,Sweatland:2025aa}. 
In these methods, the constrained control problem is explicitly formulated as a constrained optimization problem.
Therefore, complex nonlinear (even non-convex) input constraints can be systematically incorporated into the control design, which is a significant advantage over structural methods.

Even though they solve constrained optimization problems at each time instant, the computational burden may not necessarily be significant for small-scale QPs or MPC problems with short horizons and simple constraints.
However, it can increase substantially when multiple constraints must be handled simultaneously, nonlinear constraints are introduced, or the resulting problem is non-convex.
In such cases, real-time implementation may require iterative numerical solvers, suitable initialization, and careful control of the problem dimension and solver convergence.
In this regard, some studies \cite{Vahidi-Moghaddam:2025aa,Mallick:2025aa,Khodaverdian:2025aa} trained the optimal constrained control policy offline using DNNs to reduce online computation.

Moreover, closed-loop stability guarantees generally rely on additional analytical conditions specific to the selected optimization framework.
For MPC, these conditions commonly include recursive feasibility, appropriate terminal ingredients, sufficiently long prediction horizons, and computational tractability \cite{Greune:2011aa}.
In addition to these conditions, model accuracy is critical, thereby motivating robust \cite{Bemporad:1999aa}, data-driven \cite{Berberich:2025aa}, and learning-based MPC \cite{Draeger:1995aa,Vahidi-Moghaddam:2025aa} approaches.
Model accuracy is also crucial for CBF-QP filters, where the validity of the safety guarantee depends on the accuracy of the system dynamics used to formulate the barrier constraint.
Accordingly, neuro-adaptive system identifiers have been introduced to construct the CBF constraint using approximated system dynamics \cite{Sweatland:2025aa}.

Although these approaches can provide stability or safety guarantees under their stated assumptions, the guarantees typically depend on feasibility, prediction, or filtering mechanisms whose validity is affected by model accuracy, uncertainty descriptions, data quality, and offline training coverage.

% However, optimization-based methods require additional conditions to ensure closed-loop stability.
% Representatively, in MPC design, the recursive feasibility, computational tractability, and sufficiently long prediction horizons are essential \cite{Greune:2011aa}.
% Since the model accuracy is most critical across these conditions, the uncertain or unknown dynamics have motivated robust \cite{Bemporad:1999aa}, data-driven \cite{Berberich:2025aa}, and learning-based MPC \cite{Draeger:1995aa,Vahidi-Moghaddam:2025aa}.
% For CBF-QP filters, neuro-adaptive system identifiers have been employed to accurately impose the CBF constraint using approximated system dynamics \cite{Sweatland:2025aa}.
% Additionally, for computational tractability, some studies have proposed learning the optimal control policy offline using NNs \cite{Vahidi-Moghaddam:2025aa,Mallick:2025aa,Khodaverdian:2025aa}.
% Although these methods can provide stability guarantees under the stated conditions, the guarantees are typically obtained through feasibility, prediction, or filtering mechanisms that depend on model accuracy, uncertainty descriptions, data quality, or offline training coverage.

\subsection{Motivation and Proposed Approach}

Both structural and optimization-based methods are effective for input constraint handling, but they have complementary limitations, as summarized in Table~\ref{table:intro:comparison}.
Structural methods offer Lyapunov-based stability guarantees and solver-free computation, but they are limited in terms of the classes of constraints they can accommodate.
Optimization-based methods can handle general constraints, but their closed-loop stability guarantees are often tied to additional conditions and iterative solvers may be required.

These observations motivate a unified NAC framework in which more complex input constraints are incorporated directly into the online adaptation law, as presented in our previous work \cite{Ryu:2025aa}.
In the proposed CONAC framework, the NAC problem is formulated as a constrained optimization problem in which multiple convex input constraints are imposed as inequality constraints.
The constrained optimization formulation is not used to solve a control allocation problem at each time instant; instead, it is used to derive continuous-time adaptation laws for the adaptive variables.
Therefore, the proposed framework retains the Lyapunov-friendly structure of NAC while incorporating the constraint-handling capability of optimization-based methods.

\subsection{Contributions}

The main contributions of this study are:
\begin{itemize}
  % \item 
  % A constrained optimization-based neuro-adaptive control (CONAC) framework is proposed for unknown systems under multiple convex input constraints;
  % The admissible input set is represented by convex inequality constraints and embedded directly into the online adaptation law;

  \item
  Any combination of convex input constraints can be handled, thereby significantly extending the applicability of the NAC to real-world systems with complex input constraints;
  
  \item 
  The adaptation laws for adaptive variables including the DNN weights and the Lagrange multipliers are systematically derived from the formulated constrained optimization problem, and the KKT-like first-order optimality conditions are satisfied at an equilibrium of the adaptation dynamics \cite[Chap. 12, Thm. 12.1]{Nocedal:2006aa}; and

  \item 
  The closed-loop stability under the proposed CONAC is rigorously analyzed using Lyapunov theory, ensuring the UUB of the tracking error and adaptive variables.
  % The effectiveness and feasibility are validated through simulations and real-time experiments on a 2-DOF robotic manipulator.

\end{itemize}

This study extends our preliminary work in \cite{Ryu:2025ab,Ryu:2025aa}, which introduced inequality constraints on the weight and input norms within a constrained optimization framework for NAC using single-hidden-layer NNs. 
In the present work, the same framework is generalized to accommodate multiple convex input constraints and extended to DNNs, while still ensuring Lyapunov-based stability guarantees.
Furthermore, unlike the previous study, which was limited to numerical simulations, the proposed CONAC is experimentally validated through real-time implementation on a 2-DOF robotic manipulator.

\subsection{Organization}

The remainder of this paper is organized as follows. 
Section~\ref{sec:problem:formulation} presents the target system and control objective.
Section~\ref{sec:main:result} introduces the proposed CONAC and the adaptation laws. 
Section~\ref{sec:stability:analysis} analyzes the closed-loop stability.
Section~\ref{sec:val} presents a real-time implementation of CONAC on a 2-DOF robotic manipulator, compares it with existing methods.
Finally, Section~\ref{sec:conclusion} concludes the paper and discusses potential future work.

\subsubsection*{Notations}

$\otimes$ denotes the Kronecker product \cite[Chap. 7, Def. 7.1.2]{Bernstein:2009aa}.
A vector and a matrix are denoted by $\mv{x}=[x_i]_{i\in\{1,\cdots,n\}}\in\R^n$ and 
$
  \mm A
  := 
  [a_{ij}]_{
    i\in\{1,\cdots,n\},j\in\{1,\cdots ,m\}
  }\in\R^{n\times m}
  ,
$
respectively.
The $i$th column of the matrix $\mm{A}\in\R^{n\times m}$ is denoted by $\mycol_i(\mm A)$.
For the matrix $\mm{A}\in\R^{n\times m}$, $\myvec(\mm A):=(\mycol_1(\mm A)^\top,\cdots,\mycol_m(\mm A)^\top )^\top\in\R^{nm}$ denotes the vectorization of $\mm A$.
Finally, $\mm I_n$ denotes the $n\times n$ identity matrix, and $\mm 0_{n\times m}$ denotes the $n\times m$ zero matrix.

% ------------------------------------
%       SECTION PROBLEM FORMULATION
% ------------------------------------
\section{Problem Formulation} \label{sec:problem:formulation}

We consider an unknown nonlinear system modeled as 
\begin{equation} \label{eq:sys}
  \ddtt\mv{x} 
  = 
  \mv{f}(\mv{x}) 
  + 
  \mm{B}(\mv{x})\mysat(\mv{u})
  ,
\end{equation}
where $\mv{x},\mv{u}\in\R^n$ denote the state and control input vectors, respectively.
The unknown system function and control effectiveness matrix are denoted by $\mv{f}:\R^n\to\R^n$ and $\mm{B}:\R^n\to\R^{n\times n}$, respectively, which are assumed to be locally Lipschitz continuous.
The saturation function is denoted by $\mysat:\R^n\to\Omega_u$, where $\Omega_u\subset\R^n$ is the convex admissible input set.

The system \eqref{eq:sys} is subject to the following assumptions.

\begin{assum} \label{assum:B:bound}
  The control effectiveness matrix $\mm{B}(\mv{x})$ is nonsingular and bounded as   
  $
    \underline{b}\mm{I}_n
    \preceq 
    \mm{B}(\mv{x})
    \preceq 
    \bar{b}\mm{I}_n
    ,
  $
  where $\underline{b},\bar{b}\in\R_{>0}$ are unknown positive constants.
\end{assum}

\begin{assum}[{\cite[Assumption 1(b)]{Boulkroune:2010aa}}] \label{assum:B:bound:grad}
  There exists an unknown positive function $\beta(\mv{x}):\R^n\to\R_{>0}$ such that $\tfrac{1}{2}\mv{v}^\top\left(\ddtt\mm{B}(\mv{x})^{-1}\right)\mv{v} \le \beta(\mv{x})\norm{\mv{v}}^2$, $\forall \mv{v}\in\R^n$, and $\forall \mv{x}\in\R^n$. 
\end{assum}

\begin{assum} \label{assum:sat}
  The admissible input set and saturation function satisfy the following
  conditions:
  \begin{enumerate}
    \item
    The admissible input set $\Omega_u$ is defined by smooth convex
    constraint functions $c_j^u:\R^n\to\R$, $j\in\mathcal I$, as
    \begin{equation*}
      \Omega_u
      =
      \textstyle
      \bigcap_{j\in\mathcal I}
      \left\{
        \mv u\in\R^n:
        c_j^u(\mv u)\le 0
      \right\}
      ,
    \end{equation*}
    where $\mathcal I$ denotes the input constraint index set.

    \item
    The origin lies in the interior of $\Omega_u$, i.e.,
    \begin{equation*}
      c_j^u(\mv 0)
      \le
      -\underline c_j^u
      <
      0
      ,
      \qquad
      \forall j\in\mathcal I
      ,
    \end{equation*}
    where $\underline c_j^u\in\R_{>0}$ is known.

    \item
    There exists a known constant $\underline u\in\R_{>0}$ such that
    \begin{equation*}
      \inf_{\mv u\notin\Omega_u}
      \left(
        \norm{\mysat(\mv u)}
      \right)
      =
      \underline u
      .
    \end{equation*}
  \end{enumerate}
\end{assum}

% \begin{assum} \label{assum:sat}
%     The admissible input set $\Omega_u$ is defined by smooth convex constraint functions $c_j^u(\mv u)$, $\forall j\in\mathcal I$, where $\mathcal{I}$ denotes the input constraint index set, as
%     $
%         \Omega_u
%         =
%         \bigcap_{j\in\mathcal I}
%         \{\mv u\in\R^n:c_j^u(\mv u)\le0\}
%         .
%     $
%     The origin is contained in its interior, \ie $c_j^u(\mv 0) \le -\underline{c}_j^u <0$, $\forall j\in\mathcal I$, for known $\underline{c}_j^u\in\R_{>0}$.
%     Moreover, there exists known $\underline u\in\R_{>0}$ such that
%     $
%         \inf_{\mv{u}\notin\Omega_u}
%         \left(
%             \norm{\mysat(\mv u)}
%         \right)
%         =
%         \underline u
%         .
%     $
% \end{assum}
% The saturation function $\mysat(\cdot)$ satisfies $\mysat(\mv u)=\mv u$ for $\mv u\in\Omega_u$ and, for $\mv u\notin\Omega_u$, $\mysat(\mv u)$ lies on the ray from the origin through $\mv u$, as illustrated in Fig.~\ref{fig:saturation}.

By Assumption~\ref{assum:sat}, the saturation function $\mysat(\mv{u})$ can represent any combination of multiple convex input constraints, rather than simple box constraints which are conventionally considered in the NAC literature, as illustrated in Fig.~\ref{fig:saturation}.

\begin{figure}[t]
    \centering
    % \subfloat[]{
    \subfloat[Conventional input constraints.]{
        \includegraphics[width=0.5\linewidth]{figs/inputFuncConv.drawio.pdf}
        \label{fig:saturation:conv:box}
    }
    % \hfill
    % \subfloat[]{
    \subfloat[Multiple convex input constraints.]{
        \includegraphics[width=0.47\linewidth]{figs/inputFuncProp.drawio.pdf}
        \label{fig:saturation:prop:multi}
    }
    \caption{
      Comparison of conventionally considered input constraints in the NAC literature and the proposed multiple convex input constraints.
      % In Fig.~\ref{fig:saturation:conv:box}, symmetric and asymmetric box constraints are illustrated.
      % In Fig.~\ref{fig:saturation:prop:multi}, a combination of symmetric and norm constraint are illustrated.
    }
    \label{fig:saturation}
\end{figure}

Additionally, the desired trajectory $\mv{x}_d(t):\R_{\ge0}\to\R^n$ is assumed to be given and satisfies the following assumption.

\begin{assum} \label{assum:xd}
    The desired trajectory $\mv{x}_d(t)$ is sufficiently smooth and $\mv{x}_d,\ddtt\mv{x}_d$ are bounded.
    % The desired trajectory $\mv{x}_d(t)$ is sufficiently smooth and bounded, such that $\mv{x}_d\in\Omega_{x_d}:=\left\{\mv{x}_d\in\R^n : \norm{\mv{x}_d}\le \overline{x}_d\right\}$ and $\ddtt\mv{x}_d$ is bounded in a compact set $\Omega_{dx_d}$, as well.
\end{assum}

The control objective is to design a NAC for the unknown system \eqref{eq:sys} such that $\mv{x}(t)$ tracks the desired trajectory $\mv{x}_d(t)$, the multiple convex input constraints defined by $\mysat(\cdot)$ are satisfied, and closed-loop stability is guaranteed.

% ------------------------------------
%       SECTION MAIN RESULT
% ------------------------------------
\section{Proposed CONAC Framework} \label{sec:main:result}

The architecture of the proposed CONAC consists of a DNN that functions as a NAC and an adaptation process for the adaptive variables such as the DNN weights and Lagrange multipliers, as illustrated in Fig.~\ref{fig:ctrl:diagram}.
Before presenting CONAC, we first review some preliminaries in Section~\ref{sec:main:result:sub:prelim}.
Then, the controller is presented in Section~\ref{sec:main:result:sub:ctrl:design} along with the adaptation laws in Section~\ref{sec:main:result:sub:adap:laws}.

\begin{figure}[!t]
  \centering
  \includegraphics[width=.9999\linewidth]{figs/Controller.drawio.pdf}
  \caption{
    Overall architecture of the proposed CONAC.
  }
  \label{fig:ctrl:diagram}
\end{figure}

\subsection{Preliminaries} \label{sec:main:result:sub:prelim}

The following proposition will be used in the subsequent sections:
% The following propositions and lemmas will be used in the subsequent sections:

\begin{propsit}[{\cite[Prop. 7.1.9]{Bernstein:2009aa}}] \label{propsit:kron}
  For a matrix $\mm A\in\R^{n\times m}$ and a vector $\mv b\in\mathbb R^n$, the following property holds:
  \begin{equation}
    \mm A^\top \mv b 
    = 
    \myvec(\mm A^\top\mv b)
    =
    \myvec(\mv b^\top\mm A)
    = 
    (\mm{I}_{m}\otimes \mv b^\top) \myvec(\mm A)
    .
  \end{equation}
\end{propsit}

% \begin{lem}[Comparison Lemma {\cite[pp. 102--103, Lemma 3.4]{Khalil:2002aa}}] \label{lem:comp}
%     Consider a scalar continuous function $V(t)\ge0,\ \forall t\in\R_{\ge0}$.
%     If $V(t)$ satisfies the differential inequality    
%     % \begin{equation}
%     $
%         \ddtt V(t) \le -a V(t) + b
%         ,
%     % \end{equation}
%     $
%     with $a,b\in\R_{>0}$, then $V(t)$ is bounded as 
%     % \begin{equation}
%     $
%         V(t) \le \left(V(0) - \tfrac{b}{a}\right)\exp^{-at} + \tfrac{b}{a}
%         ,
%         \ \forall t\in\R_{\ge0}
%         .
%     % \end{equation}
%     $
% \end{lem}

% \begin{proof}
%     See \cite[pp. 689--660]{Khalil:2002aa}.
% \end{proof}

% Based on the Comparison Lemma (Lemma~\ref{lem:comp}), the following lemma establishes the uniform ultimate boundedness (UUB) of the state vector $\mv{z}(t):\R_{\ge0}\to\R^n$ within the constrained set $\mathcal{Z}:=\left\{\mv{z}\in\R^n : \norm{\mv{z}}\le\overline{z}\in\R_{>0}\right\}$.

% \begin{lem} \label{lem:comp:compact}
%     Consider a scalar continuous function $V(\mv{z})$, such that $\gamma_1\norm{\mv{z}}^2 \le V(\mv{z}) \le \gamma_2\norm{\mv{z}}^2$, where $\gamma_1,\gamma_2\in\R_{>0}$.
%     If $V(\mv{z})$ satisfies    
%     \begin{equation}
%         \ddtt V(\mv{z}) 
%         \le 
%         -\gamma_3\norm{\mv{z}}^2 + c
%         ,\ 
%         \forall t\in\R_{\ge0}
%         ,\ 
%         \forall \mv{z}(t)\in\mathcal{Z}
%         ,
%     \end{equation}
%     where $\gamma_3,c\in\R_{>0}$, then $\mv{z}(t),\ \forall \mv{z}\in\mathcal{Z}$, is bounded as 
%     \begin{equation}
%         \norm{\mv{z}(t)} 
%         \le 
%         \sqrt{
%             \left(
%                 \tfrac{\gamma_2}{\gamma_1}    
%                 \norm{\mv{z}(0)}^2
%                 - 
%                 \tfrac{\gamma_2 c}{\gamma_1\gamma_3}
%             \right)
%             \exp(-\tfrac{\gamma_3}{\gamma_2}t) 
%             + 
%             \tfrac{\gamma_2 c}{\gamma_1\gamma_3}
%         }
%         .
%     \end{equation}
%     Conversely, to ensure that $\mv{z}(t)\in\mathcal{Z},\ \forall t\in\R_{\ge0}$, it is sufficient to satisfy
%     % $
%     %     \sqrt{
%     %         \tfrac{\gamma_2}{\gamma_1}    
%     %         \norm{\mv{z}(0)}^2 
%     %         +
%     %         \tfrac{\gamma_2 c}{\gamma_1\gamma_3}
%     %     }
%     %     \le
%     %     \overline{z}
%     %     ,
%     % $
%     $
%         \max
%         \left(
%             \sqrt{
%                 \tfrac{\gamma_2}{\gamma_1}
%             }
%             \norm{\mv{z}(0)}
%             ,
%             \sqrt{
%                 \tfrac{\gamma_2 c}
%                 {\gamma_1\gamma_3}
%             }
%         \right)
%         \le
%         \overline{z}
%     $
%     which holds if 
%     % $
%     %     \norm{\mv{z}(0)} 
%     %     \le 
%     %     \sqrt{
%     %         \tfrac{\gamma_1}{\gamma_2}\overline{z}^2
%     %         -
%     %         \tfrac{c}{\gamma_3}
%     %     }
%     % $
%     $
%         \norm{\mv{z}(0)}
%         \le
%         \overline{z}
%         \sqrt{
%             \tfrac{\gamma_1}{\gamma_2}
%         }
%     $
%     .
% \end{lem}

% \begin{proof}
%     % Using Comparison Lemma \cite[pp. 689--660, Lemma 3.4]{Khalil:2002aa}, the proof is straightforward.
%     Using Comparison Lemma (Lemma~\ref{lem:comp}), the proof is straightforward, and thus omitted for brevity.
% \end{proof}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.99\linewidth]{figs/DNN.drawio.pdf}
  \caption{
      Example of the DNN $\NN(\NNin;\wth)$ with $k=2,l_0=2,l_1=l_2=3$, and $l_3=2$.
  }
  \label{fig:DNN}
\end{figure}

% ————————————————————————————————
% DNN Preliminaries
% ————————————————————————————————

We review the DNN structure and its approximation capability, which are essential for the subsequent controller design and stability analysis.
According to the universal approximation property \cite{Patil:2022aa}, the DNNs $\NN: \mathbb{R}^{l_0+1} \times \mathbb{R}^{\Xi} \to \mathbb{R}^{l_{k+1}}$ can be used to approximate any smooth unknown nonlinear function $\mv{g}:\R^{l_0+1}\to\R^{l_{k+1}}$.
Specifically, there exists a DNN such that 
\begin{equation} \label{eq:dnn:approx:error}
  \sup_{\NNin\in\Omega_{\NNin}}
  \norm{
    \mv{g}(\NNin) - \NN(\NNin;\wth^*)
  }
  =
  \sup_{\NNin\in\Omega_{\NNin}}
  \norm{
    \mv{\epsilon}
  }
  =
  \overline{\epsilon}
  ,
\end{equation}
where $\NNin \in \mathbb{R}^{l_0+1}$ is the DNN input vector and $\idealwth \in \mathbb{R}^{\Xi}$ denotes the ideal DNN weights that minimize the approximation error $\mv{\epsilon}\in\R^{l_{k+1}}$.
The DNN input vector $\NNin$ should be designed to include all arguments of the unknown function $\mv{g}$.
A compact set $\Omega_{\NNin} \subset \mathbb{R}^{l_0+1}$ denotes the approximation region of the DNN, which ensures the boundedness of $\idealwth$ and $\mv{\epsilon}$, such that $\norm{\idealwth} \le \overline{\theta}^*\in\R_{>0}$ and $\norm{\mv{\epsilon}} \le \overline{\epsilon} \in\R_{>0}$.
The DNN $\NN(\NNin;\wth)=\NN_k$ can be recursively defined as
\begin{equation} \label{eq:dnn:def}
  \NN_i :=
  \begin{cases}
    \wV_i^\top \act_i(\NN_{i-1}), 
    &
    i\in\{1,\dots ,k\},
    \\
    \wV_0^\top \NNin,
    &
    i=0
    ,
  \end{cases}
\end{equation}
where $\wV_i\in\R^{(l_i+1)\times l_{i+1}}$ and $\act_i: \R^{l_i}\to\R^{l_i+1}$ represent the weight matrix and the activation function of the $i$th layer for all $i\in\mathcal{K}$, respectively, and $\mathcal{K}:=\{0,1,\cdots,k\}$ denotes the index set of layers.
The DNN $\NN(\NNin;\wth)$ has $k+1$ layers, where the input and output layers are indexed by $i=0$ and $i=k$, respectively, as illustrated by the example in Fig.~\ref{fig:DNN}.
The activation function is defined as $\act_i(\mv{z}) = \left(\tanh(z_1),\cdots,\tanh(z_{l_i}),1\right)^\top,$ for some $\mv{z}\in\R^{l_i}$.
We simplify the notations by vectorizing all weight matrices as 
\begin{equation*}
  \wth_i 
  :=
  \myvec(\wV_i)
  \in\R^{(l_i+1)l_{i+1}}
  ,
  \qquad
  \forall 
  i\in\mathcal{K}
  ,
\end{equation*}
and, finally, stacking them as $\wth := \left[\wth_i\right]_{i\in\mathcal{K}}\in\R^\Xi$, where $\Xi := \sum_{i\in\mathcal{K}} (l_i+1)l_{i+1}$.
Additionally, there exists a maximum norm bound for the weights of each layer $\overline{\theta}^*_i\in\R_{>0}$, such that $\norm{\idealwth_i}\le\overline{\theta}^*_i$, $\forall i\in\mathcal{K}$.

The gradient of $ \NN(\NNin;\wth)$ with respect to $\wth$ can be obtained using Proposition~\ref{propsit:kron} and the chain rule as follows:
\begin{equation}
  \ppfrac{\NN}{\wth}=
  \begin{bmatrix}
    \ppdfrac{\NN}{\wth_k}&
    \ppdfrac{\NN}{\wth_{k-1}}&
\cdots &
    \ppdfrac{\NN}{\wth_0}
  \end{bmatrix}
  \in\R^{l_{k+1} \times \Xi}
  ,
  \label{eq:gradient:phi}
\end{equation}
where
\begin{equation}
  \ppfrac{\NN}{\wth_i} = 
  \begin{cases}
    % (
      \mm I_{l_{k+1}}
      \otimes 
      \act_{k}^\top  
    % )
    , 
    &
    i=k
    ,
    \\
    \wV_k^\top\act_{k}' 
    (
      \mm I_{l_{k}}
      \otimes  
      \act_{k-1}^\top  
    )
    , 
    & 
    i=k-1
    ,
    \\
    \vdots 
    &
    \vdots 
    \\
    \wV_k^\top\act'_{k} 
    \cdots 
    \wV_1^\top\act_1' 
    (
      \mm I_{l_1}
      \otimes 
      \NNin^\top  
    )
    , 
    &
    i = 0
    ,
  \end{cases}
  \label{eq:NN:grad}
\end{equation}
and $\act_i:= \act_i(\NN_{i-1})$ and $\act_i':= \ddtfrac{\act_i}{\NN_{i-1}}$, for all $i\in\mathcal{K}\setminus\{0\}$.

\subsection{Controller Design} \label{sec:main:result:sub:ctrl:design}

We define the tracking error as $\mv{e} = \mv{x} - \mv{x}_d$, and derive the error dynamics from \eqref{eq:sys} as
\begin{equation} \label{eq:error:dyn}
    \ddtt\mv{e}
    =
    \mv{f}(\mv{x}) + \mm{B}(\mv{x})\mysat(\mv{u}) - \ddtt\mv{x}_d
    .
\end{equation}
Let us consider the Lyapunov function $V_c:=\tfrac{1}{2}\mv{e}^\top\mm{B}^{-1}(\mv{x})\mv{e}$.
Invoking Assumptions~\ref{assum:B:bound:grad}, the time derivative of $V_c$ can be bounded as follows:
\begin{align} \label{eq:Vc:dot:1} \nonumber
  \ddtt V_c
  =&
  \tfrac{1}{2}\mv{e}^\top
  \left(
    \ddtt\mm{B}^{-1}
  \right)
  \mv{e}
  +
  \mv{e}^\top\mm{B}^{-1}
  \left(
    \mv{f} + \mm{B}\mysat(\mv{u}) 
    - \ddtt\mv{x}_d
  \right)
  \\
  \le&
  \beta(\mv{x})\norm{\mv{e}}^2
  \!+\!
  \mv{e}^\top\mv{u}
  \!+\!
  \mv{e}^\top\mm{B}^{-1}
  \left(
    \mv{f} \!-\! \ddtt\mv{x}_d
  \right)
  \!+\!
  \mv{e}^\top
  \Delta\mv{u}
  ,
\end{align}
where the arguments of $\mv{f}(\mv{x})$ and $\mm{B}(\mv{x})$ are omitted for brevity and $\Delta\mv{u} := \mysat(\mv{u}) - \mv{u}$.

A desired control law $\mv{u}_d$ such that the tracking error $\mv{e}$ is stable in the absence of input saturation is selected as 
\begin{equation} \label{eq:ctrl:ideal}
  \mv{u}_d
  :=
  -
  \mm{P}
  \mv{e}
  - 
  \beta(\mv{x})\mv{e}
  +
  \mm{B}^{-1}
  \left(
    - \mv{f} 
    + \ddtt\mv{x}_d
  \right)
  % +
  % h(\mv{x},\mv{x}_d)
  ,
\end{equation}
where $\underline{p}\mm{I}_n \preceq \mm{P}=\mm{P}^\top \preceq \overline{p}\mm{I}_n$ for known positive constants $\underline{p},\overline{p}\in\R_{>0}$.
% The function $h:\R^n\times\R^n\to\R^n$ is correction term to satisfy the ideal control law $\mv{u}^*$ within the input constraints, \ie $\mv{u}^*\in\Omega_u$.

By substituting $\mv{u}_d$ in \eqref{eq:ctrl:ideal} into \eqref{eq:Vc:dot:1}, we have
\begin{align} \label{eq:Vc:dot:2} 
  \ddtt V_c
  \le &
  -\mv{e}^\top
  \mm{P}
  \mv{e}
  +
  \mv{e}^\top
  \left(
    \mv{u} - \mv{u}_d
  \right)
  +
  \mv{e}^\top
  \Delta\mv{u}
  \\ \nonumber
  \le & 
  -
  \underline{p}\norm{\mv{e}}^2
  +
  \mv{e}^\top
  \left(
    \mv{u} - \mv{u}_d       
  \right)
  +
  \mv{e}^\top
  % \left(
    \Delta\mv{u}
%     +
%     h(\mv{x},\mv{x}_d)
  % \right)
  .
\end{align}
Notably, in the ideal case, \ie $\mv{u} = \mv{u}_d$ and $\Delta\mv{u} = \mv{0}$, the tracking error $\mv{e}$ is asymptotically exponentially stable with decay rate $\underline{p}\overline{b}$, since 
$
  \tfrac{1}{\overline{b}}
  \ddtt
  \left(
    \norm{\mv{e}}^2
  \right)
  \le
  \ddtt V_c 
  \le 
  -\underline{p}\norm{\mv{e}}^2
$.

Since the ideal control law \eqref{eq:ctrl:ideal} contains unknown functions $\beta(\mv{x})$, $\mv{f}$ and $\mm{B}$, we employ the DNN $\NN(\NNin;\wth)$ in \eqref{eq:dnn:approx:error} and \eqref{eq:dnn:def} to approximate the ideal control law as follows:
\begin{equation*}
% \begin{aligned}
  \NN(\NNin;\idealwth)
  +
  \mv{\epsilon}
  =
  -\mv{e}
  -
  \mm{P}^{-1}
  \left(
  \beta(\mv{x})\mv{e}
  +
  \mm{B}^{-1}
  (
    \mv{f}
    -
    \ddtt \mv{x}_d
  )
  \right)
  .
% \end{aligned}
\end{equation*}
% where $\mv{\epsilon}\in\R^n$ denotes an approximation error vector, such that 
% $
% \sup_{\NNin\in\Omega_{\NNin}}   
% \left( 
%   \norm{\mv{\epsilon}}
% \right)
% =
% \overline{\epsilon}
% .
% $
The DNN input vector is defined as 
$
  \NNin 
  = 
  \left(
    \mv{x}_d^\top, 
    \ddtt{\mv{x}}_d^\top, 
    \mv{x}^\top, 
    1 
  \right)^\top
  \in\R^{3n+1}
$.
Notably, the DNN input vector $\NNin$ can be defined in various ways, as long as it includes the arguments of the unknown functions.
Then, we have 
\begin{equation} \label{eq:ctrl:NN:ideal}
  \mv{u}_d
  =
  \mm{P}
  \big(
    \NN(\NNin;\idealwth)
    +
    \mv{\epsilon}
  \big)
  .
\end{equation}
The actual control law is designed as 
\begin{equation} \label{eq:ctrl:NN:actual}
  \mv{u}
  :=
  \mm{P}
  \NN(\NNin;\estwth)
  ,
\end{equation}
where $\estwth\in\R^{\Xi}$ denotes the estimated DNN weights.

By substituting \eqref{eq:ctrl:NN:ideal} and \eqref{eq:ctrl:NN:actual} into \eqref{eq:Vc:dot:2}, we have
\begin{equation} \label{eq:Vc:dot:3}
  \ddtt V_c
  \le
  - \underline{p}
  \norm{\mv{e}}^2
  +
  \mv{e}^\top\mm{P}
  \left(
    \estNN - \idealNN
    - \mv{\epsilon}
  \right)
  +
  \mv{e}^\top
  \Delta\mv{u}
  ,
\end{equation}
where $\estNN\!:=\!\NN(\NNin;\estwth)$ and $\idealNN\!:=\!\NN(\NNin;\wth^*)$ are abbreviated.

\subsection{Adaptation Law Derivation} \label{sec:main:result:sub:adap:laws}

% The control objective which minimizes the tracking error $\mv{e}$ while satisfying both weight boundedness and input constraints can be represented as follows:
The control objectives defined in Section~\ref{sec:problem:formulation} can be formulated as a constrained optimization problem, as follows:
\begin{subequations} \label{eq:opt:prob} 
  \begin{align} 
    \min_{\estwth} \quad 
    & 
    J(\mv{e};\estwth)
    = 
    \tfrac{1}{2} \mv{e}^\top \mv{e} 
    &
    \label{eq:opt:prob:obj} 
    \\ 
    \text{subject to} \quad  
    & 
    c^{\theta}_i(\estwth_i)=\tfrac{1}{2}
    \left(
      \norm{\estwth_i}^2
      -
      \overline{\theta}_i^2 
    \right)
    \le 0, 
    \!
    &
    \!
    \forall i\in\mathcal{K} 
    ,
    \label{eq:opt:prob:const1} 
    \\ 
    & 
    c^{u}_j(\estwth)
    % c_j^u
    % \left(
    %     \mm{P}\NN(\NNin;\estwth)
    % \right) 
    \le 0, 
    &
    \forall j\in\mathcal{I} 
    \label{eq:opt:prob:const2} 
    ,
  \end{align}%
\end{subequations}
where $J(\mv{e};\estwth)$ is the objective function.
Inequality constraints of weight boundedness $c^{\theta}_i(\estwth_i),\ \forall i\in\mathcal{K},$ and input saturation $c^{u}_j(\estwth),\ \forall j\in\mathcal{I},$ are imposed.

Specifically, the input constraints used in \eqref{eq:opt:prob:const2} are the same convex constraint functions that characterize the admissible input set in Assumption~\ref{assum:sat}.
With a slight abuse of notation, the composed input constraints are denoted by
$c_j^u(\estwth):=c_j^u(\mv u(\estwth))=c_j^u(\mm P\NN(\NNin;\estwth)),\ \forall j\in\mathcal I$, to explicitly represent the dependence of the input constraints on the DNN weights $\estwth$.
Additionally, all active constraints are assumed to satisfy the Linear Independence Constraint Qualification (LICQ) to avoid degeneracy \cite[pp. 465--467]{Nocedal:2006aa}.

The Lagrangian function $L:=L(\mv{e},\estwth,[\sigma_i]_{i\in\mathcal{K}},[\lambda_j]_{j\in\mathcal I})$ is defined as
\begin{equation}
  L
  = 
  J(\mv{e};\estwth) 
  + 
  \textstyle\sum_{i\in\mathcal{K}}
  \sigma_{i}
  c^{\theta}_{i}(\estwth_i)
  +
  \textstyle\sum_{j\in\mathcal I}
  \lambda_{j}
  c^{u}_{j}(\estwth)
  ,
  \label{eq:lagrangian}
\end{equation}
where $[\sigma_i]_{i\in\mathcal{K}}$ and $[\lambda_j]_{j\in\mathcal{I}}$ denote the Lagrange multipliers for the weight constraints \eqref{eq:opt:prob:const1} and input constraints \eqref{eq:opt:prob:const2}, respectively.
Then, the primal-dual problem can be formulated as follows:
\begin{equation} \label{eq:opt:prob:saddle}
  \min_{\estwth}
  \max_{
    [\sigma_i]_{i\in\mathcal{K}}
    ,
    [\lambda_j]_{j\in\mathcal{I}}
  } 
  L(\mv{e},\estwth,[\sigma_i]_{i\in\mathcal{K}},[\lambda_j]_{j\in\mathcal I})
  .
\end{equation}

The adaptation laws for the adaptive variables, $\estwth$, $[\sigma_i]_{i\in\mathcal{K}}$, and $[\lambda_j]_{j\in\mathcal{I}}$, are derived to solve \eqref{eq:opt:prob:saddle} by using a primal-dual gradient update law, \ie 
$
  \ddtt\estwth 
  = 
  -\alpha \pptfrac{L}{\estwth}
$, $
  \ddtt\sigma_i 
  = 
  \myproj_{\sigma_i\ge0}
  \left(
      \beta_i^\theta 
      \pptfrac{L}{\sigma_i}
  \right)
$, and $
  \ddtt\lambda_j 
  = 
  \myproj_{\lambda_j\ge0}
  \left(
    \beta_j^u 
    \pptfrac{L}{\lambda_j}
  \right)
$, as follows:
\begin{subequations} \label{eq:adap}
  \begin{align}
    \ddtt{\estwth}
    &
    =
    -\alpha 
    \left(
      \pptfrac{J}{\estwth}
      +
      \textstyle\sum_{i\in\mathcal{K}}
      \sigma_i 
      \estwth_i
      +
      \textstyle\sum_{j\in\mathcal{I}}
      \lambda_j 
      \pptfrac{c^{u}_{j}}{\estwth}
    \right),
    \label{eq:adap:th}
    \\
    \ddtt\sigma_i
    & 
    = 
    \myproj_{\sigma_i\ge0}
      \left(
      \beta_i^{\theta}
      c^{\theta}_{i} 
    \right)
    ,
    \qquad \forall i\in\mathcal{K},
    \label{eq:adap:sig}
    \\
    \ddtt\lambda_j
    & 
    = 
    \myproj_{\lambda_j\ge0}
      \left(
      \beta_j^{u}
      c^{u}_{j} 
    \right)
    ,
    \qquad \forall j\in\mathcal{I},
    \label{eq:adap:lbd}
  \end{align}%
\end{subequations}
where $\alpha\in\R_{>0}$ denotes the adaptation gain (learning rate) and $\beta_i^{\theta},\beta_j^{u}\in\R_{>0},\ \forall i\in\mathcal{K},j\in\mathcal{I}$, denote the update rates of the Lagrange multipliers. 
The Lagrange multipliers associated with inequality constraints are kept non-negative, \ie $\sigma_i,\lambda_j\ge 0$, due to the projection operator $\myproj_{(\cdot)\ge0}(\cdot)$ in \eqref{eq:adap:sig} and \eqref{eq:adap:lbd}, which is presented in \cite[Appendix E, eq. (E.4)]{Krstic:1995aa}.

When a constraint becomes violated, \ie positive constraint violation $c^{\theta}_{i},c^{u}_{j}\ge0$, the corresponding Lagrange multiplier increases according to \eqref{eq:adap:sig} and \eqref{eq:adap:lbd}, to address the violation by updating weight $\estwth$ through \eqref{eq:adap:th}.
Ultimately, the Lagrange multiplier exhibits two distinct behaviors depending on the constraint status: it either decreases to zero (\eg via a projection operator) when the constraint becomes inactive, $c^{\theta}_i,c^{u}_j < 0$, or converges to a positive value when the constraint remains binding, $c^{\theta}_i,c^{u}_j = 0$, to enforce the limit.

\begin{remark}[Comparison with Existing Methods for Weight Boundedness] \label{rem:weight:bound}
  Unlike projection-based methods that directly restrict the adaptive weights to a prescribed set, or $\sigma$-modification, or $\epsilon$-modification that continuously bias them toward the origin \cite{Ryu:2025ab}, CONAC handles weight boundedness through inequality constraints within the primal-dual adaptation framework like the input constraints.
  The corresponding constraint term is only activated when the prescribed bound is violated, thereby softly driving the weights toward the admissible set without direct clipping or persistent origin-directed bias.
\end{remark}

Meanwhile, the adaptation law of $\estwth$ defined in \eqref{eq:adap:th} involves the partial derivative of the error with respect to the control input $\pptfrac{\mv{e}}{\mv{u}}$, \ie 
$
    \pptfrac{J}{\estwth}
    =
    \big(
        (\pptfrac{\mv{e}}{\mv{u}})
        (\pptfrac{\mv{u}}{\estwth})
    \big)
    ^\top \mv{e}
    =
    % \left(
    \big(
        (\pptfrac{\mv{e}}{\mv{u}})
        \mm{P}
        (\pptfrac{\estNN}{\estwth})
    \big)
    % \right)
    ^\top \mv{e}
$. 
Since the objective function depends on the tracking error $\mv{e}$ of a dynamic system, obtaining $\pptfrac{\mv{e}}{\mv{u}}$ is not straightforward. 
Exact sensitivity computation methods, such as the forward sensitivity method \cite{Khalil:2002aa,Ryu:2025ab}, can be used in principle, but they are often computationally demanding for real-time applications with numerous DNN weights.
Therefore, several approximation methods have been proposed \cite{Douratsos:2007aa,Saerens:1991aa}.
They approximate $\pptfrac{\mv{e}}{\mv{u}}$ by taking the sign of each entry of the control effectiveness matrix, \ie
$
  \pptfrac{\mv{e}}{\mv{u}}
  \approx
  \big[
    \mysign
    % \left(
    (
        % \pptfrac{e_i}{u_j}
    % \right)
    b_{i,j}
    )
  \big]_{i,j\in\{1,\cdots,n\}}
  ,
$
which corresponds to the MIT rule in the context of adaptive control.
However, since the entry of the control effectiveness matrix $\mm{B}(\mv{x})$ is unknown in this study, we instead approximate $\mm{B}(\mv{x})$ by its sign, \ie $\pptfrac{\mv{e}}{\mv{u}}\approx\mysign(\mm{B}(\mv{x}))=\mm{I}_{n}$, under Assumption~\ref{assum:B:bound}.
Consequently, the adaptation law in \eqref{eq:adap:th} becomes:
\begin{equation}
  \ddtt{\estwth}
  \approx
  -\alpha 
  \left(
    {\pptfrac{\estNN}
    {\estwth}}^\top
    \mm{P}
    \mv{e}
    +
    \textstyle\sum_{i\in\mathcal{K}}
    \sigma_i 
    \estwth_i
    +
    \textstyle\sum_{j\in\mathcal{I}}
    \lambda_j 
    \pptfrac{c^{u}_{j}}{\estwth}
  \right)
  .
  \label{eq:adap:th:approx}
\end{equation}

Additionally, the adaptation law \eqref{eq:adap:th} provides an optimality interpretation at equilibrium.
At an equilibrium of the adaptation law \eqref{eq:adap}, where $\ddtt{\estwth}=0$, $\ddtt\sigma_i=0$, and $\ddtt\lambda_j=0$, the resulting equilibrium conditions correspond to KKT-like conditions, \ie $\pptfrac{L}{\estwth}\approx0$, $c^{\theta}_i,c^{u}_j \le 0$, $\sigma_i,\lambda_j \ge 0$, and $\sigma_i c^{\theta}_i=0$, and $\lambda_j c^{u}_j=0,\ \forall i\in\mathcal{K},j\in\mathcal{I}$.
Thus, the equilibria characterize candidates for locally optimal solutions of the formulated constrained adaptation problem.

% \begin{remark}[Verification of Approximated Adaptation Law]
%     In traditional adaptive control literature, by assuming known sign 

%     Furthermore, the approximated adaptation law \eqref{eq:adap:th:approx} coincides with the various existing adaptation laws in the literature when there are no constraints, \ie $\sigma_i=0,\ \forall i\in\mathcal{K}$ and $\lambda_j=0,\ \forall j\in\mathcal{I}$, derived using Lyapunov stability theory \cite{} and static approximation methods \cite{}.

% \begin{remark}[Locally Lipschitz Continuity of the Adaptation Law] \label{rem:adap:lip}
%     The approximated adaptation law \eqref{eq:adap:th:approx} is locally Lipschitz continuous under the stated regularity assumptions.
%     Specifically, the error dynamics are locally Lipschitz, the DNN Jacobian $\pptfrac{\estNN}{\estwth}$ is locally bounded due to the use of $\tanh(\cdot)$ activation functions, and the input constraints $c_j^u(\mv{u})$ are $\mathcal{C}^1$ with respect to $\estwth$, while $\mv u=\mv P\estNN(\cdot;\estwth)$ is smooth in $\estwth$.
%     This property, in conjunction with the boundedness of signals established via subsequent Lyapunov analysis in Section~\ref{sec:stability:analysis}, precludes the finite escape time of the closed-loop system under the proposed CONAC
% \end{remark}

\begin{algorithm}[!t]
  \caption{Proposed CONAC}\label{alg:CONAC}
  \begin{algorithmic}[1]
    \STATE \textbf{Initialize:} $\estwth^{(0)}$, $\sigma_i^{(0)},\ \forall i\in\mathcal{K}$, $\lambda_j^{(0)},\ \forall j\in\mathcal{I}$
    \FOR{$r = 0 $ to $\infty$}
      \STATE Obtain $\mv{x}^{(r)}$ and $\mv{x}_d^{(r)}$
      \STATE $\mv{e}^{(r)}\leftarrow\mv{x}^{(r)}-\mv{x}_d^{(r)}$

      \STATE \textit{// Neuro-adaptive control law (Section~\ref{sec:main:result:sub:ctrl:design})}

        % \STATE $\NNin^{(r)}\leftarrow
        %   \left(
        %     {\mv{x}_d^{(r)}}^\top,
        %     {\ddtt{\mv{x}}_d^{(r)}}^\top,
        %     {\mv{x}^{(r)}}^\top,
        %     1
        %   \right)^\top$
        \STATE Define $\NNin^{(r)}$ from all arguments of $\mv{u}_d^{(r)}$

        \STATE $\mv{u}^{(r)}\leftarrow\NN(\NNin^{(r)};\estwth^{(r)}) $ based on \eqref{eq:ctrl:NN:actual}
        \STATE Apply $\mv{u}^{(r)}$ to the system in \eqref{eq:sys}

      \STATE \textit{// Update DNN weights (Section~\ref{sec:main:result:sub:adap:laws})}

      \STATE Compute $\pptfrac{\NN}{\estwth^{(r)}}$ as defined in \eqref{eq:gradient:phi}
      \STATE Compute $c^{\theta}_i,c^{u}_j$ and $\pptfrac{c^{u}_{j}}{\estwth^{(r)}}$ for all $i\in\mathcal{K}, j\in\mathcal{I}$
      
      \STATE Update $\estwth^{(r+1)}$, $\sigma_i^{(r+1)},\ \forall i\in\mathcal{K}$, and $\lambda_j^{(r+1)},\ \forall j\in\mathcal{I}$ using \eqref{eq:adap:th:approx}, \eqref{eq:adap:sig}, and \eqref{eq:adap:lbd}, respectively.
    \ENDFOR
  \end{algorithmic}
\end{algorithm}

The proposed CONAC algorithm is presented in Algorithm~\ref{alg:CONAC}, implemented in a discrete-time controller with a sampling time, where $(\cdot^{(r)})$ denotes the value at the $r$th time step.

% ------------------------------------
%       SECTION STABILITY ANALYSIS
% ------------------------------------
\section{Stability Analysis} \label{sec:stability:analysis}

In this section, the stability of the closed-loop system under the proposed CONAC is analyzed.
To facilitate the stability analysis, we introduce the following lemmas.

\begin{lem}[Convexity of input constraints with respect to $\estwth_k$] \label{lem:convexity:input:constraint}
  The input constraint function $c_j^{u}(\estwth_k),\ \forall j \in \mathcal{I},$ is convex with respect to the output layer weights $\estwth_k$, resulting in the inequality 
  \begin{equation*}
    c_j^{u}(\estwth_k) 
    - 
    \pptfrac{c_j^{u}}{\estwth_k}^\top 
    \estwth_k 
    \le 
    % c_j^{u}(\estwth)\big|_{\estwth_k=\mv{0}}
    - \underline{c}_j^{u}
    <
    0
    .
  \end{equation*}
\end{lem}

\begin{proof}
  % The dependence of the preceding layers and $\NNin$ on the output layer appears through the activation vector $\act_k$.
  % According to the DNN gradient in \eqref{eq:gradient:phi}, we have
  According to Proposition~\ref{propsit:kron}, the output of the DNN $\estNN$ depends linearly on the output layer weights $\estwth_k$ as follows:
  \begin{equation} \label{eq:NN:linear}
    \estNN
    =
    \estwV_k^\top
    \estact_k
    = 
    \left(
      \mm{I}_{l_{k+1}}
      \!
      \otimes
      \! 
      \estact_k^\top
    \right)
    \myvec(\estwV_k)
    = 
    \left(
      \mm{I}_{l_{k+1}} 
      \!
      \otimes 
      \!
      \estact_k^\top
    \right)
    \estwth_k
    .
  \end{equation}
  This implies that the control input $\mv{u}=\mm{P}\estNN$ depends linearly on $\estwth_k$.
  Therefore, the convexity of $c_j^u(\estwth),\ \forall j \in \mathcal{I}$, with respect to $\mv{u}$ is preserved with respect to $\estwth_k$, due to the linear dependence of $\mv{u}$ on $\estwth_k$.
  In addition, the convexity of $c_j^u(\estwth)$ with respect to $\estwth_k$ implies 
  % \begin{equation*}
  $
    c_j^{u}(\estwth_k) 
    - 
    \pptfrac{c_j^{u}}{\estwth_k}^\top 
    \estwth_k 
    \le 
    c_j^{u}(\estwth)\big|_{\estwth_k=\mv{0}}
    % - \underline{c}_j^{u}
    % <
    % 0
    .
  $
  % \end{equation*}
  % , where $c_j^u(\mv{0})$ denotes the value at $\estwth_k=\mv{0}$
  Additionally, since $\estwth_k=\mv{0}$ implies $\mv{u}=\mm{P}\estNN=\mv{0}$, (\ie output layer weight is zero), we have $c_j^{u}(\estwth)\big|_{\estwth_k=\mv{0}}=-\underline{c}_j^{u}<0$ by Assumption~\ref{assum:sat}, which concludes the proof.
\end{proof}

\begin{lem}[Boundedness of $\Delta\mv{u}$ with respect to $\estwth_k$] \label{lem:delta:u:bound}
  The saturated amount of control input $\Delta\mv{u}$ is bounded by $\estwth_k$ as $\norm{\Delta\mv{u}}\le \overline{p}\sqrt{l_k+1}\norm{\estwth_k}$.
\end{lem}

\begin{proof}
  Since $\act_k(\cdot)$ employs the bounded activation function $\vert\tanh(\cdot)\vert < 1$, we have 
  \begin{equation}
    \norm{\act_k(\cdot)} \le \sqrt{l_k+1}
    ,
  \end{equation}
  for any output of the preceding layers and $\NNin$.
  Hence, invoking the relation in \eqref{eq:NN:linear}, the control input $\mv{u}=\mm{P}\estNN$ is bounded by $\estwth_k$ as follows:
  \begin{equation} \label{eq:NN:bound}
    \norm{\mv{u}} 
    \le
    \overline{p}
    \norm{\estNN}
    \le
    \overline{p}
    \sqrt{l_k+1}\norm{\estwth_k}
    .   
  \end{equation}

  In addition, since there exists $\underline{u}>0$ such that $\inf_{\mv u\notin\Omega_u}\allowbreak(\norm{\mysat(\mv u)})=\underline{u}$ by Assumption~\ref{assum:sat}, we have
  $
    \norm{\Delta\mv u}
    \le
    \norm{\mv u}-\underline{u}
    ,
  $ 
  while $\Delta\mv u=\mv 0$ when $\mv u\in\Omega_u$.
  Thus, we have
  \begin{equation}
    \norm{\Delta\mv{u}}
    \le
    \max
    \left\{
      \overline{p}\sqrt{l_k+1}\norm{\estwth_k}
      -
      \underline{u}
      ,
      0
    \right\}
    \le
    \overline{p}\sqrt{l_k+1}\norm{\estwth_k}
    ,  
  \end{equation}
  which concludes the proof.    
\end{proof}

\begin{lem} \label{lem:proj:ineq}
  For any $a,b\in\R_{\ge0}$ and $c\in\R_{>0}$, the following inequality holds:
  \begin{equation}
    (a-b)\myproj_{a\ge0}(c) \le (a-b) c.
  \end{equation}
\end{lem}

\begin{proof}
  The inequality can be verified by considering the two cases of $a>0$ and $a=0$, as follows:
  \begin{equation*}
    (a-b)\myproj_{a\ge0}(c) 
    =
    \begin{cases}
      (a-b) c, & \text{if } a>0,
      \\
      0, & \text{if } a=0,
    \end{cases}
  \end{equation*}
  This implies that $(a-b)\myproj_{a\ge0}(c) \le (a-b) c$ always holds and concludes the proof.
\end{proof}

The following theorem establishes the UUB of the tracking error $\mv{e}$, the DNN weights $\estwth$, and the Lagrange multipliers $\sigma_i,\ \forall i \in \mathcal{K}$, and $\lambda_j,\ \forall j \in \mathcal{I}$.

\begin{theorem} \label{thm:stability}
  Consider the closed-loop system consisting of \eqref{eq:sys}, the control law \eqref{eq:ctrl:NN:actual}, and the adaptation laws \eqref{eq:adap}.
  Suppose that Assumptions~\ref{assum:B:bound}--\ref{assum:sat} hold and the initial condition belongs to the compact invariant set 
  $
  \bigcap_{i\in\mathcal K}
  \mathcal S_i(\overline V_i)
  $ 
  constructed in \eqref{eq:Sk:def} and \eqref{eq:Si:def}.
  Then, the tracking error $\mv e$, the DNN weights $\estwth$, and the Lagrange multipliers $\sigma_i,\ \forall i \in \mathcal{K},$ and $\lambda_j,\ \forall j \in \mathcal{I},$ are uniformly ultimately bounded.
\end{theorem}

\begin{proof}

The proof is divided into two steps.
We first analyze the UBB of the tracking error $\mv{e}$, the output-layer adaptive variables, $\estwth_k$, and $\sigma_k$, and the Lagrange multipliers associated with the input constraints $\lambda_j,\ \forall j\in\mathcal{I}$.
This step provides the key boundedness result for the tracking error and the controller output.
In the following step, we analyze the remaining inner-layer adaptive variables, $\estwth_i$ and $\sigma_i,\ \forall i\in\mathcal{K}\setminus\{k\}$.

\hfill

\textit{Step~1: Boundedness of the tracking error, output-layer and input-constraint adaptive variables.}

Consider the following Lyapunov function 
\begin{equation} \label{eq:Vk:def}
  V_{k}(\mv{z}_k)
  :=
  V_c
  +
  \tfrac{1}{2\alpha}\estwth_k^\top\estwth_k
  +
  \tfrac{1}{\beta_k^\theta}(\sigma_k-\sigma_k^o)^2
  +
  \textstyle\sum_{j\in\mathcal{I}}
  \tfrac{1}{2\beta_j^{u}}\lambda_j^2
  ,
\end{equation}
where 
$
  \mv{z}_k
  :=
  \left(
    \mv{e}^\top,
    \estwth_k^\top,
    \sigma_k,
    \left[
      \lambda_j
    \right]_{j\in\mathcal{I}}^\top
  \right)^\top
$ and $\sigma_k^o\in\R_{>0}$ will be determined later.
The Lyapunov function $V_k:=V_k(\mv{z}_k)$ is lower bounded and radially unbounded with respect to $\mv{z}_k$.
In the following analysis, we consider the case where the output layer weight constraint is violated, \ie $c_k^{\theta}(\estwth_k)>0$.
If this condition does not hold, then $c_k^{\theta}(\estwth_k)\le0$ implies $\norm{\estwth_k}\le\overline{\theta}_k$, and the boundedness of $\estwth_k$ follows directly.

The time derivative of $V_{k}$ can be analyzed as follows:
\begin{align} \label{eq:Vk:dot:1} \nonumber
  \ddtt V_{k}
  \le&
  -\underline{p}
  \norm{\mv{e}}^2
  +
  \mv{e}^\top\mm{P}
  \left(
    \estNN - \idealNN
    - \mv{\epsilon}
  \right)
  +
  \mv{e}^\top
  \Delta \mv{u}
  \\ \nonumber
  &
  -
  \underbrace{
    \estwth_k^\top
    \pptfrac{\estNN}{\estwth_k}^\top
    \mm{P}
    \mv{e}
  }_{
    = 
    \estNN^\top\mm{P}\mv{e}
    ,\ \text{see, \eqref{eq:gradient:phi}, \eqref{eq:NN:linear}}
  }
  -
  \sigma_k
  \estwth_k^\top
  \estwth_k
  -    
  \textstyle\sum_{j\in\mathcal{I}}
  \lambda_j
  \estwth_k^\top
  \pptfrac{c^{u}_j}{\estwth_k}
  \\ % \nonumber
  &
  +
  2(\sigma_k-\sigma_k^o) c_k^{\theta}(\estwth_k)
  +
  \textstyle\sum_{j\in\mathcal{I}}
  \lambda_j c^{u}_j(\estwth)
  \\ \nonumber
  \le&
  -\underline{p}
  \norm{\mv{e}}^2
  +
  \mv{e}^\top
  \mm{P}
  \left(
    - \idealNN
    - \mv{\epsilon}
  \right)
  +
  \mv{e}^\top
  \Delta \mv{u}
  -
  \sigma_k \norm{\estwth_k}^2
  \\ \nonumber
  &
  +
  (\sigma_k-\sigma_k^o) 
  \left(
    \norm{\estwth_k}^2
    -
    \overline{\theta}_k^{2}
  \right)
  +
  \underbrace{
    \textstyle\sum_{j\in\mathcal{I}}
    \lambda_j
    c^{u}_j(\estwth)\big\vert_{\estwth_k=\mv{0}}
  }_{\text{see, Lemma~\ref{lem:convexity:input:constraint}}}
  \\ \nonumber
  =&
  -\underline{p}
  \norm{\mv{e}}^2
  +
  \mv{e}^\top
  \mm{P}
  \left(
    - \idealNN
    - \mv{\epsilon}
  \right)
  +
  \mv{e}^\top
  \Delta \mv{u}
  \\ \nonumber
  &
  -
  \sigma_k^o
  \norm{\estwth_k}^2
  -
  \sigma_k
  \overline{\theta}_k^2
  +
  \sigma_k^o
  \overline{\theta}_k^2
  -
  \textstyle\sum_{j\in\mathcal{I}}
  \lambda_j
  \underline{c}_j^u 
  .
\end{align}
The projection operators in \eqref{eq:adap:sig} and \eqref{eq:adap:lbd} are omitted according to Lemma~\ref{lem:proj:ineq}.

For the boundedness of $\idealwth_k$ and $\mv{\epsilon}$, we assume that $\NNin$ remains in the approximation region $\Omega_{\NNin}$, which will be ensured later in the proof.
Hence, $\idealwth_k$ and $\mv{\epsilon}$ are bounded by $\overline{\theta}_k^*$ and $\overline{\epsilon}$, respectively.
Similarly to the bound of $\estNN$, $\idealNN$ is bounded by $\overline{\theta}_k^* \sqrt{l_k+1}$.

Using Lemma~\ref{lem:delta:u:bound}, we can further analyze $\ddtt V_{k}$ as follows:
\begin{align} \label{eq:Vk:dot:2} 
  \ddtt V_{k}
  \le&
    -\underline{p}
    \norm{\mv{e}}^2
    -
    \sigma_k^o
    \norm{\estwth_k}^2
  \\ \nonumber
  &
    +
    \overline{p}
    \norm{\mv{e}}
    \left(
      \overline{\theta}^*_k
      \sqrt{l_{k}+1}
      +
      \overline{\epsilon}
      +
      \sqrt{l_k+1}\norm{\estwth_k}
    \right)        
  \\ \nonumber
  &
    -
    \sigma_k 
    \overline{\theta}_k^2
    +
    \sigma_k^o 
    \overline{\theta}_k^2
    -
    \textstyle\sum_{j\in\mathcal{I}}
    \lambda_j
    \underline{c}_j^u
    .
\end{align}

For further analysis, we apply Young's inequality which states 
% \begin{equation}
$
  ab
  \le
  \eta a^2
  +
  \frac{1}{4\eta}b^2,
  \ 
  \forall a,b\in\R, \eta\in\R_{>0}
  .
$
% \end{equation}
By applying Young's inequality to the third term in the right-hand side of \eqref{eq:Vk:dot:2}, we have
\begin{align} \label{eq:Vk:dot:3} \nonumber
  \ddtt V_{k}
  \le &
    -(\underline{p}-\eta_1-\eta_2)
    \norm{\mv{e}}^2
    -
    \left(
      \sigma_k^o
      \!-\!
      \tfrac{1}{4\eta_2}
      \overline{p}^2
      (l_k+1)
    \right)
    \norm{\estwth_k}^2
  \\ 
  &
    -
    \sigma_k \overline{\theta}_k^2
    -
    \textstyle\sum_{j\in\mathcal{I}}
    \lambda_j
    \underline{c}_j^u
  \\ \nonumber
  &
      +
  \underbrace{
    \tfrac{1}{4\eta_1}
    \left[
    \overline{p}
      \left(
        \overline{\theta}_k^*
        \sqrt{l_{k}+1}
        +
        \overline{\epsilon}
      \right)
    \right]^2
    +
    \sigma_k^o
    \overline{\theta}_k^2
  }_{=: \delta_k \in \R_{>0}}
  ,
\end{align}
where $\eta_1,\eta_2\in\R_{>0}$ and $\sigma_k^o\in\R_{>0}$ are arbitrarily chosen such that $\underline{p}-\eta_1-\eta_2>0$ for given $\underline{p}$, and $\sigma_k^o - \frac{1}{4\eta_1} \overline{p}^2 (l_k+1) > 0$, respectively.

This result implies that $\ddtt V_{k}$ is negative outside the compact set defined by 
\begin{equation} 
  \Omega_k
  \!
  =
  \!
  \left\{
    \!
    \mv{z}_k
    \!
    :
    \!
    a
    \!
    \norm{
      \left(
        \!
        \mv{e}^\top
        \!
        ,
        \!
        \estwth_k^\top
        \!
      \right)^\top
    }^2
    \!
    +
    \!
    b
    \!
    \norm{
      \left(
        \sigma_k
        % \!
        ,
        % \!
        [\lambda_j]_{j\in\mathcal{I}}^\top
      \right)^\top
    }_1
    \!
    \le
    \!
    \delta_k
    \!
  \right\}
  \!
  ,
\end{equation}
where
$
    a
    :=
    \min
    \left\{
        \underline{p}
        - \eta_1
        - \eta_2
        ,
        \sigma_k^o
        -
        \tfrac{1}{4\eta_1}
        \overline{p}^2
        (l_k+1)
    \right\}
    \in\R_{>0}
    ,
    b
    :=
    \min
    \left\{
        \overline{\theta}_k^2
        ,
        \min_{j\in\mathcal{I}} 
        \left\{
            \underline{c}_j^u
        \right\}
    \right\}
    \in\R_{>0}
    .
$
% and
% $
%     \mv{z}_1 
%     := 
%     \left(
%         \mv{e}^\top
%         ,
%         \estwth_k^\top
%     \right)^\top
%     ,
%     \mv{z}_2
%     :=
%     \left(
%         \sigma_k
%         ,
%         [\lambda_j]_{j\in\mathcal{I}}^\top
%     \right)^\top
%     .
% $
Moreover, since $V_k$ is lower bounded and radially unbounded, there exists a compact sublevel set
\begin{equation} \label{eq:Sk:def}
  \mathcal S_k(\overline V_k)
  :=
  \left\{ 
    \mv{z}_k 
    : 
    V_k(\mv{z}_k)
    \le 
    \overline{V}_k 
  \right\}
  ,
\end{equation}
where $\overline{V}_k\in\R_{>0}$ is arbitrarily chosen such that $\Omega_k \subset \mathcal{S}_k(\overline{V}_k)$.
Because $\ddtt V_k<0$ for all $\mv{z}_k\notin\Omega_k$, the system initialized in $\mathcal{S}_k(\overline{V}_k)$ will never escape $\mathcal{S}_k(\overline{V}_k)$ and eventually enter $\Omega_k$.
Therefore, UBB of $\mv e$, $\estwth_k$, $\sigma_k$, and $\lambda_j$, $\forall j\in\mathcal I$, is ensured in $\mathcal{S}_k(\overline{V}_k)$.

In addition, the boundedness of $\mv{z}_k$ implies that $\mv{x}$ is bounded, since $\mv{e}=\mv{x}-\mv{x}_d$ and $\mv{x}_d$ is bounded by Assumption~\ref{assum:xd}.
Then, by defining the approximation region $\Omega_{\NNin}$ as $\mathcal{S}_k(\overline{V}_k)$, we can ensure that $\NNin$ remains in $\Omega_{\NNin}$.
This justifies the boundedness of $\idealwth_k$ and $\mv{\epsilon}$ used in this analysis.

\hfill

\textit{Step~2: Boundedness of the inner-layer adaptive variables.}

The remaining weights $[\estwth_i]_{i\in\mathcal{K}\setminus\{k\}}$ and the Lagrange multipliers $[\sigma_i]_{i\in\mathcal{K}\setminus\{k\}}$ are analyzed recursively from the $(k-1)$th layer to the input layer ($i=0$). 
Specifically, for an arbitrary layer index $i\in\{0,\cdots,k-1\}$, we assume that the subsequent adaptive variables, $\estwth_{i+1},\sigma_{i+1},\cdots,\estwth_k,\sigma_k,$ have already been shown to be bounded, and then prove the boundedness of $\estwth_i$ and $\sigma_i$.

Consider the Lyapunov function of the $i$th layer defined as
\begin{equation} \label{eq:Vi:def}
  V_{i}(\mv{z}_i)
  :=
  \tfrac{1}{2\alpha}\estwth_i^\top\estwth_i
  +
  \tfrac{1}{\beta_i^\theta}(\sigma_i-\sigma_i^o)^2
  ,
\end{equation}
where 
$
  \mv{z}_i:=
  \left(
    \estwth_i^\top,
    \sigma_i
  \right)^\top
  ,
$
and $\sigma_i^o\in\R_{>0}$ will be determined in the subsequent analysis.
As in the analysis of $V_k$, the weight constraint $c_i^{\theta}(\estwth_i)$ is assumed to be violated, and the projection operator in \eqref{eq:adap:sig} is omitted according to Lemma~\ref{lem:proj:ineq}.
Taking the time derivative of $V_i$ yields
\begin{align} \label{eq:Vi:dot:1} 
  \ddtt V_i
  \le&
    -\sigma_i \norm{\estwth_i}^2
    -
    \estwth_i^\top
    {\pptfrac{\estNN}{\estwth_i}}^\top
    \mm{P}
    \mv{e}
  \\ \nonumber
  &
    -
    \textstyle\sum_{j\in\mathcal{I}}
    \lambda_j 
    \estwth_i^\top
    {\pptfrac{\estNN}{\estwth_i}}^\top
    \mm{P}
    \pptfrac{c^{u}_{j}}{\mv{u}}
    +
    2(\sigma_i-\sigma_i^o) c_i^{\theta}(\estwth_i)
  \\ \nonumber
  \le&
    -\sigma_i \norm{\estwth_i}^2
    +
    (\sigma_i-\sigma_i^o) 
    \left(
      \norm{\estwth_i}^2
      -
      \overline{\theta}_i^2
    \right)
  \\ \nonumber
  &
    -
    \estwth_i^\top
    \underbrace{
    \left(
      {\pptfrac{\estNN}{\estwth_i}}^\top
      \mm{P}
      \left(
        \mv{e}
        +
        \textstyle\sum_{j\in\mathcal{I}}
        \lambda_j 
        \pptfrac{c^{u}_{j}}{\mv{u}}
      \right)
    \right)
    }_{=: \gamma_i\in\R^{\Xi_i}}
  \\ \nonumber
  \le&
    -\sigma_i^o \norm{\estwth_i}^2
    -\sigma_i \overline{\theta}_i^2
    +
    \norm{\estwth_i}
    \norm{\gamma_i}
    +
    \sigma_i^o \overline{\theta}_i^2
  .
\end{align}

For further analysis, the boundedness of $\gamma_i$ is analyzed.
According to the DNN gradient in \eqref{eq:gradient:phi}, $\pptfrac{\estNN}{\estwth_i}$ is bounded, since $\estwth_{i+1},\cdots,\estwth_k$ are bounded by assumption.
The remaining terms $\mv e$, $\estwth_k$, and $[\lambda_j]_{j\in\mathcal I}$ are bounded by the result of Step~1.
Furthermore, $\mv u$ is bounded because $\mv u$ is bounded by $\estwth_k$ in \eqref{eq:NN:bound}.
In addition, since $c_j^u(\cdot)$ is smooth by Assumption~\ref{assum:sat} and $\mv u$ is bounded, $\pptfrac{c_j^u}{\mv u}$ is bounded.
Therefore, $\gamma_i$ is bounded by an unknown positive constant $\overline{\gamma}_i\in\R_{>0}$.

Then, by applying Young's inequality to the coupled term $\norm{\estwth_i}\norm{\gamma_i}\le\norm{\estwth_i}^2+\tfrac{1}{4}\overline{\gamma}_i^2$ in \eqref{eq:Vi:dot:1}, we have
\begin{align} \label{eq:Vi:dot:2}
  \ddtt V_i
  \le&
    -
    \left(
      \sigma_i^o
      -
      1
    \right)
    \norm{\estwth_i}^2
    -
    \sigma_i \overline{\theta}_i^2
    +
    \delta_i
  ,
\end{align}
where $\delta_i:=\frac{1}{4}\overline{\gamma}_i^2 + \sigma_i^o \overline{\theta}_i^2$ is a positive constant, and $\sigma_i^o\in\R_{>0}$ is arbitrarily chosen such that $\sigma_i^o - 1 > 0$.
As in Step~1, $\ddtt V_i$ is negative outside the compact set defined by 
\begin{equation}
  \Omega_i
  =
  \left\{
    \mv{z}_i
    :
    \left(
      \sigma_i^o
      -
      \tfrac{\overline{\gamma}_i^2}{2}
    \right)
    \norm{\estwth_i}^2
    +
    \overline{\theta}_i^2
    \norm{\sigma_i}_1
    \le
    \delta_i
  \right\}
  , 
\end{equation}
which implies the boundedness of $\estwth_i$ and $\sigma_i$.
Moreover, a compact sublevel set $\mathcal{S}_i(\overline{V}_i)$ of $V_i$ can be constructed such that 
\begin{equation} \label{eq:Si:def}
  \mathcal{S}_i(\overline{V}_i)
  :=
  \left\{
    \mv{z}_i
    :
    V_i(\mv{z}_i)
    \le
    \overline{V}_i
  \right\}
  ,
\end{equation}
where $\overline{V}_i\in\R_{>0}$ is arbitrarily chosen such that $\Omega_i \subset \mathcal{S}_i(\overline{V}_i)$.
Since $\ddtt V_i<0$ for all $\mv{z}_i\notin\Omega_i$, the system initialized in $\mathcal{S}_i(\overline{V}_i)$ will remain in $\mathcal{S}_i(\overline{V}_i)$.

By recursively applying the above analysis from $i=k-1$ to $i=0$, the boundedness of $\estwth_i$ and $\sigma_i$ for all $i\in\mathcal{K}\setminus\{k\}$ can be established, which concludes the proof.

\end{proof}

% ——————————————————————————————————————————————
% REMARKS
% ——————————————————————————————————————————————

Additionally, we introduce the following remarks regarding the initialization and the parameter tuning of CONAC.

\begin{remark}[Initialization of CONAC] \label{rem:init}
  The invariant sublevel set 
  $
    \bigcap_{i\in\mathcal{K}} 
    \mathcal{S}_i(\overline{V}_i)
  $ 
  used in the stability analysis depends on unknown constants.
  Therefore, the exact admissible initial domain cannot be computed explicitly.
  In practice, we recommend initializing; the system state $\mv{x}(0)$ close to the initial reference state $\mv{x}_d(0)$ to reduce the initial tracking error $\mv{e}(0)$; DNN weights with small random values; and zero for the Lagrange multipliers, $[\sigma_i(0)]_{i\in\mathcal{K}}=\mv{0}$ and $[\lambda_j(0)]_{j\in\mathcal{I}}=\mv{0}$ without constraint violations, to help ensure that the system starts within
  $
    \bigcap_{i\in\mathcal{K}} 
    \mathcal{S}_i(\overline{V}_i)
    .
  $
\end{remark}

\begin{remark}[Initial Desired Trajectory Design] \label{rem:ref}  
  The initialization in Remark~\ref{rem:init} helps prevent initial unstable behavior, which generally results from poor initial performance of NAC due to the randomly initialized DNN weights.
  With small random weights, the initial control input is expected to be small, thereby avoiding abrupt control actions.
  However, if the system starts near an unstable or highly nonlinear operating region, small random weights may result in unstable behavior or slow adaptation.
  Hence, it is preferable to design the desired trajectory such that the system starts in a sufficiently stable operating region, and use a conservative desired trajectory during an initial warm-up phase, as introduced in \cite{Ryu:2025aa}.
  % This strategy is demonstrated across valuations in Section~\ref{sec:val}.
\end{remark}

\begin{remark}[Tuning of CONAC Parameters] \label{rem:tuning}
  The adaptation gain $\alpha$ and the multiplier update rates $\beta_i^\theta$ and $\beta_j^u$ affect the transient behavior of CONAC.
  The gain $\alpha$ determines the overall update speed of the DNN weights.
  A large $\alpha$ may improve the initial learning response but can also induce overshooting or oscillatory behavior, resulting in failure to learn the ideal weights.
  Conversely, an excessively small $\alpha$ may lead to slow adaptation and poor transient tracking performance.
  The update rates $\beta_i^\theta$ and $\beta_j^u$ determine how rapidly the Lagrange multipliers respond to weight and input constraint violations, respectively.
  Larger values accelerate the enforcement of the corresponding constraints by increasing the multiplier response when a violation occurs.
  However, excessively large update rates may introduce abrupt changes in the adaptation law, leading to chattering or high-frequency oscillations in the control input on the constraint boundary.

  Therefore, $\alpha$, $\beta_i^\theta$, and $\beta_j^u$ should be selected to balance learning speed, prompt constraint enforcement, and smooth closed-loop behavior.
  The effect of the input constraint multiplier update rate $\beta_j^u$ is evaluated in the parametric study in Section~\ref{sec:val:results}.
\end{remark}

% ------------------------------------
%       SECTION VALIDATIONS
% ------------------------------------
\section{Validations} \label{sec:val}

\subsection{Validation Setup} \label{sec:val:setup}

% -------------------------------------
% Experiment Setup
% -------------------------------------
To validate the proposed CONAC, a real-time experiment was conducted on a two-link robotic manipulator, as shown in Fig.~\ref{fig:exp:real:set}. 
The OpenCR1.0 board \cite{opencr} with a 32-bit ARM Cortex processor and a $\qty{216}{\mega\hertz}$ clock frequency was used.
The two-link robotic manipulator was actuated by two Cubemars AK10-90 servo motors.
The servos were controlled using the OpenCR1.0 board, which transmits the torque reference to the corresponding servo via controller area network (CAN) communication at a sampling rate of $\qty{500}{\hertz}$ using a PCAN-USB interface.
The system parameters and the schematic diagram are shown in Table~\ref{table:exp:system:params} and Fig.~\ref{fig:exp:robot:diagram}, respectively.

% -------------------------------------
% Robot System
% -------------------------------------
The two-link robotic manipulator dynamics are described by the following Euler-Lagrange equations:
\begin{equation} \label{eq:robot:dynamics}
  \mv{M}(\mv{q})\tfrac{\der^2}{\der t^2}{\mv{q}}
  +
  \mv{C}(\mv{q},\ddtt\mv{q})\ddtt\mv{q}
  +
  \mv{G}(\mv{q})
  =
  \mysat
  (
    \mv{\tau}
  )
  ,
\end{equation}
where $\mv{q} := \left(q_1,q_2\right)^\top \in \R^2$ is the joint angle vector, $\mv{\tau} := \left(\tau_1,\tau_2\right)^\top \in \R^2$ is the joint torque vector, $\mv{M}(\mv{q}) \in \R^{2\times2}$ is the inertia matrix, $\mv{C}(\mv{q},\ddtt\mv{q}) \in \R^{2\times2}$ is the Coriolis matrix, and $\mv{G}(\mv{q}) \in \R^2$ is the gravity vector.
The inertia matrix $\mv{M}(\mv{q})$ whose inverse is the control effectiveness matrix, is a symmetric positive-definite matrix according to the physical properties of Euler-Lagrange systems, \cite[Lemma 3.1]{Ji:2025aa}.

To apply CONAC, the system is reformulated into the first-order dynamics form in \eqref{eq:sys} by defining the state as $\mv{x} := \ddtt\mv{q}+\Lambda\mv{q}$, and the control input as $\mv{u} := \mv{\tau}$, where $\Lambda\in\R^{2\times2}$ is a positive definite matrix, and $\mv{f}:=\mv{M}^{-1}(\mv{q})\left(-\mv{C}(\mv{q},\ddtt\mv{q})\ddtt\mv{q}-\mv{G}(\mv{q})\right)+\Lambda\ddtt\mv{q}$, and $\mm{B}:=\mv{M}^{-1}(\mv{q})$.
Moreover, the DNN input vector is defined as 
$
  \NNin 
  := 
  \left(
    (\mv{q}-\mv{q}_d)^\top
    ,
    \ddtt(\mv{q}-\mv{q}_d)^\top
    ,
    \tfrac{\der^2}{\der t^2}\mv{q}_d^\top
    ,
    1
  \right)^\top
  ,
$
since $\mv{f}$, $\mm{B}$ and $\ddtt\mv{x}_d$ in \eqref{eq:ctrl:NN:actual} are functions of $\mv{q},\mv{q}_d,\ddtt\mv{q},\ddtt\mv{q}_d$ and $\tfrac{\der^2}{\der t^2}\mv{q}_d$.

The input saturation $\mysat(\mv{\tau})$ is defined as a combination of the torque saturation of the second joint and the norm saturation of the control input, as follows:
\begin{equation} \label{eq:exp:sat}
  \mysat(\mv{\tau})
  =
  \begin{cases}
    % if |tau_2| > \overline{\tau}_2 and |tau_1| > \overline{\tau}_1
    \begin{pmatrix}
      \mysign(\tau_1)\overline{\tau}_1
      \\
      \mysign(\tau_2)\overline{\tau}_2
    \end{pmatrix}
    , &
    \text{if }
    \vert\tau_2\vert > \overline{\tau}_2
    \text{ and }
    \vert\tau_1\vert > \overline{\tau}_1
    \\
    % if |tau_2| > \overline{\tau}_2 and |tau_1| <= \overline{\tau}_1
    \left(
      \tau_1
      , 
      \mysign(\tau_2)\overline{\tau}_2
    \right)^\top
    , &
    \text{if }
    \vert\tau_2\vert > \overline{\tau}_2
    \text{ and }
    \vert\tau_1\vert \le \overline{\tau}_1
    \\
    % if |tau_2| <= \overline{\tau}_2 and ||tau|| > \overline{\tau}
    \tfrac{\overline{\tau}}{\norm{\mv{\tau}}}\mv{\tau}
    , &
    \text{if }
    |\tau_2| \le \overline{\tau}_2
    \text{ and }
    \norm{\mv{\tau}} > \overline{\tau}
    \\
    % otherwise
    \mv{\tau}
    , &
    \text{otherwise}
    ,
  \end{cases}
  % \mysat(\mv{\tau})
  % =
  % \min\left\{
  %   1,\,
  %   \frac{\overline{\tau}}{\norm{\mv{v}}}
  % \right\}
  % \mv{v}
  % ,
  % % \text{where } 
  % \ 
  % \mv{v}
  % :=
  % \begin{pmatrix}
  %   \tau_1
  %   \\
  %   \operatorname{clip}(\tau_2,-\overline{\tau}_2,\overline{\tau}_2)
  % \end{pmatrix}
  % ,
\end{equation}
where $\overline{\tau}_2=\qty{3.8}{\newton\meter}$, $\overline{\tau}_1=\qty{10.32}{\newton\meter}$ and $\overline{\tau}=\qty{11}{\newton\meter}$, as illustrated in Fig.~\ref{fig:exp:robot:sat}.
The saturation function can be decomposed into three convex input constraints, which can be physically interpreted as the upper and lower torque limit of the second joint and the power source limit, respectively, and are denoted by
\begin{equation} \label{eq:val:cstr:input}
  c^u_{\overline{\tau}_2}(\mv{\tau}) 
  := 
  \tau_2 - \overline{\tau}_2
  ,
  \ 
  c^u_{\underline{\tau}_2}(\mv{\tau}) 
  := 
  -\tau_2 - \overline{\tau}_2
  ,
  \ 
  c^u_{\overline{\tau}}(\mv{\tau})
  := 
  \norm{\mv{\tau}}^2 - \overline{\tau}^2
  .
\end{equation}
Accordingly, the input constraint set is defined as $\mathcal{I} = \{\overline{\tau}_2, \underline{\tau}_2, \overline{\tau}\}$.

\begin{figure}[t]
\centering
  \includegraphics[width=.99\linewidth]
  % \includegraphics[width=\figSizeOneCol\linewidth]
  {
    figs/expSet.drawio.png
  }%
\caption{
    Experimental setup of the two-link robotic manipulator used for real-time validation.
  }
\label{fig:exp:real:set}
\end{figure}

\begin{figure}[t]
  \centering
  \subfloat[Schematic diagram of the two-link robotic manipulator.]{
    \includegraphics[width=0.46\linewidth]{figs/RobotModel.drawio.pdf}
    \label{fig:exp:robot:diagram}
  }
  % \vfill
  \subfloat[Considered input Saturation.]{
    \includegraphics[width=0.48\linewidth]{figs/saturation.drawio.pdf}
    \label{fig:exp:robot:sat}
  }
  \caption{
    The two-link robotic manipulator and the considered input saturation.
  }
  \label{fig:exp:robot}
\end{figure}

\begin{table}[t]
  \renewcommand{\arraystretch}{1.3}
  \caption{Two-link robotic manipulator's parameters.}
  \centering
  \begin{tabular}{c m{11.5em} c}
  \hline
  \textbf{Symbol} & \textbf{Description} & \textbf{Value} \\
  \hline
  \hline 
    $m_1,m_2$ & Mass of the links & \qty{2.465}{\kilo\gram} \\
  \hline
    $l_1,l_2$  & Length of the links & \qty{0.2}{\meter} \\  
  \hline
    ${l_c}_1,{l_c}_2$ & Length to the center of mass & \qty{0.139}{\meter} \\
  \hline
    $I_1,I_2$  & Inertia of the links & \qty{0.069}{\kilo\gram\meter\squared} \\
  \hline
  \end{tabular}
  \label{table:exp:system:params}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.7\linewidth]{figs/desired_traj.drawio.pdf}
  \caption{
    The desired trajectory in Episode~1 and Episode~2.
  }
  \label{fig:exp:des:traj}
\end{figure}

% ------------------------------------
% desired trajectory
% ------------------------------------
The desired trajectory comprised 4 segments that move the system from 
$
  \mv{q}_d^{(0)}
  :=
  \left(
    -\qty{60}{\degree}
    ,
    \qty{60}{\degree}
  \right)^\top    
$
to 
$
  \mv{q}_d^{(1)}
  :=
  \left(
    \qty{45}{\degree}
    ,
    -\qty{90}{\degree}
  \right)
  ,
$ 
then to 
$
  \mv{q}_d^{(2)}
  :=
  \left(
    -\qty{45}{\degree}
    ,
    \qty{45}{\degree}
  \right)^\top
  ,
$
back to
$
  \mv{q}_d^{(3)}
  :=
  \mv{q}_d^{(1)}
  ,
$
and finally to the initial position $\mv{q}_d^{(0)}$, as illustrated in Fig.~\ref{fig:exp:des:traj}.
Each segment was designed using a quintic ($5$th-order) polynomial trajectory to ensure the smoothness of the desired trajectory, with zero initial and final velocities.
The time duration of the segments was defined as $\qty{2}{\second}$.
To demonstrate the learning capability of the DNN, the desired trajectory was applied twice, referred to as Episode $1$ and Episode $2$, respectively.

As discussed in Remark~\ref{rem:ref}, a warm-up phase was introduced prior to applying the desired trajectory to avoid the instability caused by the initial poor performance of the DNN.
In the warm-up phase, the manipulator was initially positioned at a stable equilibrium point, denoted as 
$
    {\qd}^{\rm{origin}} 
    := 
    \left(
        % -\tfrac{1}{2}\pi
        -\qty{90}{\degree}
        , 
        % 0
        \qty{0}{\degree}
    \right)^\top
    ,
$
and remained at this position for $\qty{3}{\second}$.
Subsequently, a conservative desired trajectory was applied to gently excite the system, which was designed as a quintic polynomial trajectory that moves the system from ${\qd}^{\rm{origin}}$ to $\mv{q}_d^{(0)}$ in $\qty{3}{\second}$, followed by a constant reference that holds the system at $\mv q_d^{(0)}$ for the next $\qty{8}{\second}$, before applying the desired trajectory.

\hfill

% -------------------------------------
% Comparative Controllers
% -------------------------------------

% 왜 optimization-based가 배제되었나
% - 우리는 structural methods를 기반으로힘
% - point-wise constraint handling이 아닌 adaptation 

For a comparative analysis, four controllers were implemented, with their properties summarized in Table~\ref{table:controller}.
In the comparative study, optimization-based methods, such as MPC and CBF-QP, were not included because the focus is on NAC-based controllers that handle input constraints.
This choice isolates the proposed contribution, namely the integration of multiple convex input constraints into the online adaptation law of NAC while retaining a Lyapunov-based stability analysis.
In addition, among the structural methods, the auxiliary system-based NAC is included as a comparative controller, since it has better input constraint handling capability than smooth approximation methods \cite{Min:2021aa}.

\begin{table}[t]
  \renewcommand{\arraystretch}{1.3}
  \caption{Controllers and parameters used for comparative analysis.}
  \label{table:controller}
  \centering
  \begin{tabular}{m{.6cm} m{4cm} m{2.7cm}}
  % \begin{tabular}{c l l}
  \hline
    &\bf{Description and parameters}&\bf{Input constraint}\\
  \hline
  \hline
    \multirow{4}{*}{
      \shortstack{
        {(C$_1$)}
        \\
        \protect\colorLine{blue}{solid}
      }
    }& Proposed CONAC  
    & 
      \multirow{4}{*}{
        \shortstack[l]{
          $-\overline{\tau}_2 \le \tau_2 \le \overline{\tau}_2$, 
          \\
            $\norm{\mv{\tau}} \le \overline{\tau}$,
          \\
            ($\overline{\tau}_2=3.8, \overline{\tau}=11$)
        }
      }
    \\
    & $\beta^{u}_{\overline{\tau}_2}=\beta^{u}_{\underline{\tau}_2}=1000$, $\beta^{u}_{\rbu}=100$ & 
    \\
    & $\mm{P} = \mm{I}_2, \overline{\theta}_0=\overline{\theta}_1=\overline{\theta}_2=15$ & 
    \\
    & $\beta^{\theta}_0=\beta^{\theta}_1=\beta^{\theta}_2=1$ &
    \\
  \hline
    \multirow{3}{*}{
      \shortstack{
        {(C$_2$)}
        \\
        \protect\colorLine{my_cyan}{solid}
      }
    }& Proposed CONAC (smaller $\beta_j$) &  
    \\
    & $\beta^{u}_{\overline{\tau}_2}=\beta^{u}_{\underline{\tau}_2}=50$, $\beta^{u}_{\rbu}=10$ & same as (C$_1$) 
    \\ 
    & Others: same as (C$_1$) & 
    \\
\hline
    \multirow{4}{*}{
      \shortstack{  
      (C$_3$)
      \\
      \protect\colorLine{magenta}{solid}
      }
    }
    & 
      NAC with auxiliary system 
    &
      \multirow{4}{*}{
        \shortstack[l]{  
        $-\overline{\tau}_1 \le \tau_1 \le \overline{\tau}_1$,
        \\
        $-\overline{\tau}_2 \le \tau_2 \le \overline{\tau}_2$, 
        \\
        ($\overline{\tau}_1=10.32, \overline{\tau}_2=3.8$)
        }
      }
    \\
    & 
    \cite{Rezaei:2025aa,Liu:2025aa,Yu:2022aa} 
    & 
    \\
    & 
    $\mm{A}_{\zeta} = \mydiag(120,100),$ 
    & 
    \\
    & 
    $\mm{B}_{\zeta} = \mydiag(5\times10^3,5\times10^3)$ 
    &
    \\
  \hline
    \multirow{2}{*}{
    \shortstack{  
      (C$_4$) 
      \\
      \protect\colorLine{gray}{solid}
    }
    }& NAC without input constraints \cite{Patil:2022aa} & 
    \multirow{2}{*}{Input is unconstrained}
    \\
    & $\overline{\theta}=15$  & 
    \\  
\hline
\hline
    \multicolumn{3}{m{8.1cm}}{
      DNN and other parameters for all controllers
    }
    \\
    \multicolumn{3}{m{8.1cm}}{
      $
        l_0=6,l_1=4,l_2=4,l_3=2,
        \alpha=0.2,
        \estwth(0)\sim\mathcal{U}\bigl([-0.5,0.5]^{\Xi}\bigr)
      $
    }
    \\
    \multicolumn{3}{m{8.1cm}}{
      $\NNin = 
      \left(
        (\mv{q}-\mv{q}_d)^\top,
        \ddtt(\mv{q}-\mv{q}_d)^\top,
        \tfrac{\der^2}{\der t^2}\mv{q}_d^\top,
        1
      \right)^\top$
      ,
      $\mm{\Lambda} = \mydiag(6,13)$
    }
    \\
\hline
  \end{tabular}
\end{table}

% ------------------------------------
% Controllers: CONAC 
% ------------------------------------
\subsubsection*{Controller 1 \normalfont{(C$_1$)}—\textit{Proposed CONAC}}

To handle all imposed input saturation, the proposed CONAC was implemented with the input bound constraints $c^u_{\overline\tau_2}$ and $c^u_{\underline\tau_2}$ for the second link, and the input norm constraint $c^u_{\rbu}$ (defined in \eqref{eq:val:cstr:input}), allowing the controller to fully utilize the authority of the actuator.
As described in \eqref{eq:ctrl:NN:actual}, the input (torque) corresponds to the output of the DNN, and the adaptation law is defined in \eqref{eq:adap}.
All parameters of (C$_1$) are listed in Table~\ref{table:controller}.
% Additionally, the weight norm constraints $c^\theta_{i},\ \forall i\in \mathcal{K}$, defined in \eqref{eq:opt:prob:const1}, were also incorporated into CONAC framework with $\overline\theta_0=\overline\theta_1=\overline\theta_2=6$.
% The update rates of the Lagrange multipliers were set to $\beta_{\theta_i}=1,\ \forall i\in\mathcal{K}$, and $\beta_{\overline{\tau}_2} = \beta_{\underline{\tau}_2} = 100$ and $\beta_{\rbu} = 10$, for the weight and input constraints, respectively.

% ------------------------------------
% Controllers: CONAC with small beta
% ------------------------------------
\subsubsection*{Controller 2 \normalfont{(C$_2$)}—\textit{Proposed CONAC with small update rate $\beta^u_j$ for $\lambda_j,\ \forall j\in\mathcal{I}$}}

For the parametric study on $\beta_j^u$ mentioned in Remark~\ref{rem:tuning}, the proposed CONAC was implemented with the same input constraints as in (C$_1$), but with smaller update rates for the input constraints, as listed in Table~\ref{table:controller}.
All other properties and parameters were kept identical to those of (C$_1$).

% ------------------------------------
% Controllers: Aux. system in CONAC-AUX
% ------------------------------------
\subsubsection*{Controller 3 \normalfont{(C$_3$)}—\textit{NAC with auxiliary system}}

As a baseline for comparison in handling input constraints, the auxiliary system used in \cite{Rezaei:2025aa,Liu:2025aa,Yu:2022aa} was adopted.
% Gao:2017aa,Esfandiari:2014aa,Esfandiari:2015aa,Liu:2024aa,Liu:2025aa,Yu:2022aa,Rezaei:2025aa
The auxiliary system is defined by 
\begin{equation}
  \ddtt \mv{\zeta} 
  =
  -
  \mm{A}_{\zeta}
  \mv{\zeta}
  +
  \mm{B}_{\zeta}
  \Delta_{\zeta} \mv{\tau}
  ,
  \quad
  \zeta(0) 
  = 
  \mv{0}_{2\times 1}
  ,
\end{equation}
where $\mv{\zeta}\in\R^2$ denotes the auxiliary state which is initialized to zero, $\mm{A}_{\zeta}\in\R_{>0}^{2\times 2}$ is a stable matrix, $\mm{B}_{\zeta}\in\R^{2\times 2}$ is a gain matrix, and $\Delta_{\zeta} \tau_i = \tau_i - \max\{\min\{\tau_i, \overline{\tau}_i\}, -\overline{\tau}_i\},\ \forall i\in\{1,2\}$.
% \operatorname{clip}(u_i, \underline{u}_i, \overline{u}_i),\ \forall i\in\{1,2\}$.
It is notable that $\Delta_{\zeta}\mv{u}=\Delta_{\zeta}\mv{\tau}$ is defined as the element-wise input overflow, which is different from the actual input saturation $\Delta\mv{u}$ in \eqref{eq:Vc:dot:1}, as illustrated in Fig.~\ref{fig:exp:sat:cmp}.
% The limits of the control input are set as $\underline{u}_1=-\overline{u}_1=10.428$ and $\underline{u}_2=-\overline{u}_2=3.5$, to approximate the input norm constraint $\overline{\tau} = 11$.
The auxiliary state was used in the adaptation law \eqref{eq:adap:th:approx}, by substituting the tracking error $\mv{e}$ with the auxiliary error $\mv{e}_\zeta := \mv{e} + \mv{\zeta}$.
The parameters of the auxiliary system were chosen to balance the responsiveness to input overflow with the convergence speed of the auxiliary dynamics, as listed in Table~\ref{table:controller}.
All other properties and parameters were kept the same as in (C$_1$) and (C$_2$).

% ------------------------------------
% Controllers: NAC without ctrl cstr
% ------------------------------------
\subsubsection*{Controller 4 \normalfont{(C$_4$)}—\textit{NAC without input constraints}}

The fourth controller, adopted from \cite{Patil:2022aa}, does not handle input constraints.
The controller structure is identical to (C$_1$)--(C$_3$), except that the input constraints were removed and weight boundedness was handled using the projection operator, as follows:
\begin{equation}
    \ddtt\estwth
    =
    \myproj_{\norm{\estwth}\le\overline{\theta}}
    \left(
        -\alpha
        \pptfrac{\estNN}{\estwth}^\top
        \mm{P}
        \mv{e}
    \right)
    .
\end{equation}
The weight norm bound was set to $\overline{\theta}=15$.
All other properties and parameters were kept the same as in (C$_1$)--(C$_3$), as summarized in Table~\ref{table:controller}.

\hfill

% ------------------------------------
% Controller Parameters
% ------------------------------------
For all controllers, the remaining parameters, including the DNN architecture, were kept identical to ensure a fair comparison, as listed in Table~\ref{table:controller}.
Specifically, invoking Remark~\ref{rem:init}, the DNN weights were initialized with small random values according to $\estwth(0)\sim\mathcal{U}\bigl([-0.5,0.5]^{\Xi}\bigr)$, where $\mathcal{U}(\cdot)$ denotes a uniform random distribution.

\subsection{Validation Results} \label{sec:val:results}

\begin{figure*}[t]
  \centering
  \subfloat[Tracking performance of $1$st joint angle $q_1$.]{
    \includegraphics[width=.49\linewidth]
    {
      \resultFig/Fig1.eps
    }%
    \label{fig:ctrl:result:general:q1}}
  \hfill
  \subfloat[Tracking performance of $2$nd joint angle $q_2$.]{
    \includegraphics[width=.49\linewidth]
    {
      \resultFig/Fig2.eps
    }%
    \label{fig:ctrl:result:general:q2}}
  \vfill
  % \vfill
  \subfloat[Commanded control input of the $1$st joint $\tau_1$.]{
  \includegraphics[width=.49\linewidth]
    {
      \resultFig/Fig5.eps
    }%
    \label{fig:ctrl:result:general:tau:1}}
  \hfill
    \subfloat[Commanded control input of the 2nd joint $\tau_2$.]{
    \includegraphics[width=.49\linewidth]
    {
      \resultFig/Fig6.eps
    }%
    \label{fig:ctrl:result:general:tau:2}}
  \vfill
  \subfloat[Norm of the commanded control input $\rbu$.]{
    \includegraphics[width=.49\linewidth]
    {
      \resultFig/Fig7.eps
    }%
    \label{fig:ctrl:result:general:tau:norm}}
  \hfill
  % \vfill
  \subfloat[Norm of DNN weights ${\estwth}_i$.]{
    % \includegraphics
    \includegraphics[width=.49\linewidth]
    {
      \resultFig/Fig10.eps
    }%
    \label{fig:ctrl:result:general:weight}}        
  \caption{
      Real-time implementation results of (C$_1$) \protect\colorLine{blue}{solid}, (C$_2$) \protect\colorLine{my_cyan}{solid}, (C$_3$) \protect\colorLine{magenta}{solid}, (C$_4$) \protect\colorLine{gray}{solid}, and desired trajectory $\qd$ \protect\colorLine{red}{dashed}.
      % \vspace{-5mm}
  }
\label{fig:ctrl:result:general}
\end{figure*}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.99\linewidth]{\resultFig/Fig21.eps}
  \caption{
    Quantitative comparison of tracking performance.
  }
  \label{fig:ctrl:results:tracking}
\end{figure}

% \begin{table}[!t]
%   \renewcommand{\arraystretch}{1.3}
%   \caption{Quantitative comparison of tracking performance.}
%   \centering
%   \begin{tabular}{c c c c c}
%   \hline
%   & \multicolumn{4}{c}{\textbf{Episode 1}} \\
%   \hline
%   \hline 
%   \qty{}{\deg} & {(C$_1$)} \protect\colorLine{blue}{solid} & {(C$_2$)} \protect\colorLine{my_cyan}{solid} & {(C$_3$)} \protect\colorLine{magenta}{solid} & {(C$_4$)} \protect\colorLine{gray}{solid} \\
%   \hline
%     $q_1-{q_d}_1$
%     & \CONAChighEpiOneAngOne & \CONAClowEpiOneAngOne & \AUXEpiOneAngOne & \NACEpiOneAngOne \\ 
%   \hline
%     $q_2-{q_d}_2$
%     & \CONAChighEpiOneAngTwo & \CONAClowEpiOneAngTwo & \AUXEpiOneAngTwo & \NACEpiOneAngTwo \\
%   \hline
%     & \multicolumn{4}{c}{\textbf{Episode 2}} \\
%   \hline
%   \hline
%   \qty{}{\deg} & {(C$_1$)} \protect\colorLine{blue}{solid} & {(C$_2$)} \protect\colorLine{my_cyan}{solid} & {(C$_3$)} \protect\colorLine{magenta}{solid} & {(C$_4$)} \protect\colorLine{gray}{solid} \\
% \hline
%   \multirow{2}{*}{$q_1-{q_d}_1$} 
%     & \CONAChighEpiTwoAngOne & \CONAClowEpiTwoAngOne & \qty{\AUXEpiTwoAngOne}{\radian} & \NACEpiTwoAngOne \\
%     & (\CONAChighImpAngOne\%) & (\CONAClowImpAngOne\%) & (\AUXImpAngOne\%) & (\NACImpAngOne\%) \\
%   \hline
%   \multirow{2}{*}{$q_2-{q_d}_2$} 
%     & \CONAChighEpiTwoAngTwo & \CONAClowEpiTwoAngTwo & \AUXEpiTwoAngTwo & \NACEpiTwoAngTwo \\
%     & (\CONAChighImpAngTwo\%) & (\CONAClowImpAngTwo\%) & (\AUXImpAngTwo\%) & (\NACImpAngTwo\%) \\
%   \hline
%   \end{tabular}
%   \label{table:results:tracking}
% \end{table}

% --------------------------------------------
% val. result: tracking performance
% --------------------------------------------

\subsubsection{Tracking and Adaptation Performance}

% 초기화 + 웜업 페이즈 + 일반적인 학습의 성능

The real-time implementation results are illustrated in Fig.~\ref{fig:ctrl:result:general}.
As shown in Fig.~\ref{fig:ctrl:result:general:q1} and Fig.~\ref{fig:ctrl:result:general:q2}, all controllers, except (C$_4$), tracked the desired trajectory with their tracking performance improving from
Episode~1 to Episode~2.
It should be noted that, due to the input saturation effect, the tracking performance of all controllers is limited.
A quantitative comparison is summarized in Fig.~\ref{fig:ctrl:results:tracking}, using the root mean square error (RMSE) of the tracking error for each joint angle during 2 episodes.
As shown in Fig.~\ref{fig:ctrl:results:tracking}, all controllers, except (C$_4$), improved their tracking performance from Episode $1$ to Episode $2$ by around 30\% and 70\% for the first and second joint angles, respectively, demonstrating the learning capability of the DNN.

Remarkably, the proposed CONAC (C$_1$) and (C$_2$) demonstrated superior tracking performance compared to the other controllers.
This is because (C$_3$) could not fully utilize the actuator authority due to the auxiliary system design, which will be discussed in detail in Section~\ref{sec:val:results:constraint}.
For (C$_4$), since it does not consider the input saturation in the adaptation process, the DNN weights were continuously increased in an attempt to reduce the tracking error, even though the corresponding control input could not be realized by the saturated actuators.
Consequently, the DNN weights exhibited persistent growth rather than convergence as shown in Fig.~\ref{fig:ctrl:result:general:weight}, which may eventually lead to parameter drift or unbounded weight growth and is therefore undesirable.

% --------------------------------------------
% val. result: constraint performance
% --------------------------------------------

\subsubsection{Constraint Handling Performance} \label{sec:val:results:constraint}

\begin{figure}[t]
  \centering
  \subfloat[Trajectory of the tracking error of each joint angle $\mv{q}$ during the time interval {$[\zoomStartTime, \qty{\zoomEndTime}{\second}]$}.]{
    \includegraphics[width=0.97\linewidth]
    {
      \resultFig/Fig16.eps
    }
    \label{fig:ctrl:result:detail:q:top:view}
  }
  \vfill
  \subfloat[Trajectory of the commanded control input $\mv{\tau}$ during the time interval {$[\zoomStartTime, \qty{\zoomEndTime}{\second}]$}.]{
    \includegraphics[width=0.97\linewidth]
    {
      \resultFig/Fig8.eps
    }
    \label{fig:ctrl:result:detail:u:top:view}
  }
  \vfill
  \subfloat[Lagrange multiplier associated with the input norm constraint for (C$_1$) and (C$_2$) during the time interval {$[\zoomStartTime, \qty{\zoomEndTime}{\second}]$}.]{
    \includegraphics[width=0.97\linewidth]
    {
      \resultFig/Fig9.eps
    }
    \label{fig:ctrl:result:detail:multipliers}
  }
  \caption{  
    Zoomed-in view of the tracking error, commanded control input, and
Lagrange multiplier $\lambda_{\overline{\tau}}$ during the time interval
$[\zoomStartTime, \qty{\zoomEndTime}{\second}]$ of (C$_1$) \protect\colorLine{blue}{solid}, (C$_2$) \protect\colorLine{my_cyan}{solid}, (C$_3$) \protect\colorLine{magenta}{solid}, and (C$_4$) \protect\colorLine{gray}{solid}.
  }
  \label{fig:ctrl:result:detail}
\end{figure}

% 여러 제약조건을 다루는 것에 대한 유연성
% 제약조건을 지속적으로 위반되더라도 최적점을 찾아가는 것

In this section, the constraint-handling performance of the controllers is analyzed.
For a detailed analysis, we focus on the time interval $[\zoomStartTime, \qty{\zoomEndTime}{\second}]$ of Episode $2$ which is highlighted by the orange lines in Fig.~\ref{fig:ctrl:result:general}, where the difference in the considered input constraints among the controllers is most pronounced.
As shown in Fig.~\ref{fig:ctrl:result:detail:q:top:view}, the tracking errors of all controllers were observed to be increased due to the input saturation effect, but (C$_1$) and (C$_2$) exhibited smaller tracking errors compared to (C$_3$) and (C$_4$).
In Fig.~\ref{fig:ctrl:result:detail:u:top:view}, the commanded control inputs of (C$_1$) and (C$_2$) were observed to properly handle the input constraints, using the full authority of the actuator.
In contrast, the commanded control input of (C$_3$) was saturated at the box constraints, which is attributed to the design of the auxiliary system that only constrains the element-wise input overflow, rather than the actual input saturation, as illustrated in Fig.~\ref{fig:exp:sat:cmp}.
Finally, the commanded control input of (C$_4$) was observed to exceed the input limits, which is expected since (C$_4$) does not consider input constraints in its adaptation process.

\begin{figure}[t]
\centering
  \caption{Comparison of the considered input saturation in (C$_1$), (C$_2$), and (C$_3$).}
  \includegraphics[width=.89\linewidth]
  % \includegraphics[width=\figSizeOneCol\linewidth]
  {
    figs/saturation_cmp.drawio.pdf
  }%
  \label{fig:exp:sat:cmp}
\end{figure}

A parametric study of the Lagrange multiplier update rates is conducted by comparing (C$_1$) and (C$_2$), focusing on the multiplier associated with the input norm constraint $\lambda_{\overline{\tau}}$.
In Fig.~\ref{fig:ctrl:result:detail:multipliers}, $\lambda_{\overline{\tau}}$ of (C$_1$) and (C$_2$) was observed to be non-zero, indicating that the input norm constraint was active and enforced during this time interval.
Specifically, the Lagrange multiplier $\lambda_{\overline{\tau}}$ of (C$_1$), which had a larger update rate, repeatedly grew and decayed, while that of (C$_2$) remained relatively constant.
Consequently, a larger update rate $\beta^u_{\overline{\tau}}$ provides a faster response to the input constraint but induces greater oscillations in the Lagrange multiplier, whereas a smaller update rate yields smoother but less responsive constraint enforcement.
This result confirms the trade-off discussed in Remark~\ref{rem:tuning}.

% Additionally, the nearly constant value of
% $\lambda_{\overline{\tau}}$ in (C$_2$) suggests that the adaptation
% dynamics are approaching an equilibrium at which the Lagrange
% multiplier converges to a nonzero value.
% This behavior is consistent with the KKT-like optimality conditions in
% \eqref{eq:ctrl:opt:KKT}, which state that, at an equilibrium, a positive
% Lagrange multiplier is associated with an active constraint satisfying
% $c_{\overline{\tau}}^u=0$ through the complementary slackness condition
% $\lambda_{\overline{\tau}}c_{\overline{\tau}}^u=0$.
% This interpretation is further supported by the commanded input norm
% remaining close to its constraint boundary during the same interval.

% --------------------------------------------
% val. result: real-time feasibility
% --------------------------------------------

\subsubsection{Feasibility of CONAC in Real-Time Implementation}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.99\linewidth]{\resultFig/Fig12.eps}
  \caption{
    Computation time of (C$_1$) \protect\colorLine{blue}{solid}, (C$_2$) \protect\colorLine{my_cyan}{solid}, (C$_3$) \protect\colorLine{magenta}{solid}, and (C$_4$) \protect\colorLine{gray}{solid} on the OpenCR1.0 board.
  }
  \label{fig:ctrl:result:cmp:time}
\end{figure}

The feasibility of the real-time implementation is analyzed in terms of the computation time.
As shown in Fig.~\ref{fig:ctrl:result:cmp:time}, the computation time of all controllers was consistently below the sampling time of $\qty{500}{\micro\second}$, confirming the feasibility of real-time implementation at a sampling frequency of $\qty{500}{\hertz}$ ($\qty{2}{\milli\second}$ maximum sampling time).
This is attributed to the fact that the proposed CONAC does not require solving an optimization problem at each time step, but rather continuously adapts the adaptive variables to solve the optimization problem in an online manner.

\subsection{Reproducibility and Video Demonstration}

For reproducibility, the source code and the experimental data are available at \url{https://github.com/KAIST-MIC-Lab/CONAC}.
In addition, the video of the real-time experiment is available at \url{https://youtu.be/?????????}.

% ------------------------------------
%       SECTION CONCLUSION
% ------------------------------------
\section{Conclusion} \label{sec:conclusion}

This paper proposed CONAC for unknown MIMO systems, addressing multiple convex input constraints within a unified constrained optimization framework. 
The adaptation law was derived from the constrained optimization problem, which was formulated to minimize the tracking error while satisfying the multiple convex input constraints.
The stability of CONAC was analyzed using Lyapunov theory, demonstrating the boundedness of the tracking errors and adaptive variables, including the DNN weights and Lagrange multipliers.
By deriving adaptation laws from the constrained optimization problem, CONAC ensured weight convergence in accordance with the KKT-like optimality conditions, thereby ensuring both stability and optimality.

Real-time implementation demonstrated the flexibility of CONAC in handling multiple convex input constraints simultaneously, and revealed the effect of the update rates of the Lagrange multipliers on the enforcement of input constraints.
In addition, the experiments validated the feasibility of CONAC in real-time implementation.

Future work may extend this approach to include constraints ons system states, and minimize the infinite horizon error cost, thereby further enhancing the flexibility and optimality of NAC systems within constrained optimization frameworks.

% -------------------------------------
% Appendix
% -------------------------------------
% \appendix

% ------------------------------------
% Bibliography
% ------------------------------------
\bibliographystyle{IEEEtran}
% \bibliographystyle{apalike}
\bibliography{localRefs, fromLR}

% ------------------------------------
% Biography
% ------------------------------------
% \vspace{-10mm}
\begin{IEEEbiography}[{
    \includegraphics[width=1in,height=1.25in,clip,keepaspectratio]{\template/people/msRyu.jpg}}]{Myeongseok Ryu}
    received the B.S. degree in Mechanical Engineering from Incheon National University, South Korea, in 2023, and the M.S. degree in Mechanical Engineering from GIST, South Korea, in 2025. 
    He is currently pursuing the Ph.D. degree in the Cho Chun Shik Graduate School of Mobility at Korea Advanced Institute of Science and Technology (KAIST), South Korea.
    His research interests include neuro-adaptive control, online optimization, and contraction theory.
    % \vspace{-10mm}
\end{IEEEbiography}

\begin{IEEEbiography}[{

\includegraphics[width=1in,height=1.25in,clip,keepaspectratio]{\template/people/dhHong.jpg}}]{Donghwa Hong}
    received his B.S. degree in Physics and Engineering Physics from Yonsei University, Wonju, South Korea, in 2023, and the M.S. degree in Mechanical and Robotics Engineering from GIST, Gwangju, South Korea, in 2026. Following his M.S., he is working in the Cho Chun Shik Graduate School of Mobility at KAIST as a researcher. His research interests include online learning and model identification. 
    % \vspace{-10mm}
\end{IEEEbiography}

\begin{IEEEbiography}[{
    \includegraphics[width=1in,height=1.25in,clip,keepaspectratio]{\template/people/khChoi.jpg}}]{Kyunghwan Choi}
    received his B.S., M.S., and Ph.D. degrees in Mechanical Engineering from KAIST, Daejeon, South Korea, in 2014, 2016, and 2020, respectively. Following his Ph.D., he worked at the Center for Eco-Friendly \& Smart Vehicles at KAIST as a Postdoctoral Fellow and later as a Research Assistant Professor. In 2022, he joined GIST as an Assistant Professor in the Department of Mechanical and Robotics Engineering. Currently, he serves as an Assistant Professor at the Cho Chun Shik Graduate School of Mobility at KAIST, where he is also the Director of the Mobility Intelligence and Control Laboratory. His research focuses on optimal and learning-based control for connected, automated, and electrified vehicles (CAEVs).
\end{IEEEbiography}

\end{document}
